rootProject.name = "turboism-root"

include(
    "runtime",
    "sdk",
    "plugins:demo",
    "plugins:ui-theme",
    "plugins:perf-opt",
    "plugins:render-opt",
    "testframework",
    "tests"
)
