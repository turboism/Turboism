[EN / English](CHANGELOG.md) · [ZH / 简体中文](CHANGELOG_zh.md) · [JP / 日本語](CHANGELOG_ja.md) · [KR / 한국어](CHANGELOG_ko.md)

# 変更履歴

Turboism のすべての重要な変更はこのファイルに記録されています。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に従い、バージョンはセマンティックバージョニングに従います。

## [Unreleased]

### 追加

- SDK とランタイムにアニメーションワークスペース対応を追加：プラグインはアニメーション
  ドキュメント、プロジェクトタイムライン、トラック、属性、キーフレームを列挙し、シーンの
  アクティブ化と名前変更、再生シーク、バッチキーフレーム編集とカーブタイプの適用、評価値の
  記録/ベイクができます。純粋な SDK の `Motion3Validator` が motion3 データの構造的問題を
  報告します。このオブジェクトモデルは `animation-timeline-host-probe` により Cubism
  5.2.03、5.3.02、5.3.03 で実機検証済みです。
- `PluginContext.availableServices()` と `PluginService` 列挙型が、ランタイムが実際に
  インストールしたオプションのコンテキストサービスを報告するため、プラグインは getter を
  試したり `unavailable()` センチネルを推測したりする必要がなくなりました。デフォルトは
  空集合でフェイルクローズします。
- `TurboismWindowFactory.installWindowIcon` がプロセス全体のウィンドウアイコン上書きを
  インストールし、すべてのプラグイン所有ウィンドウが、バンドルされたデフォルトではなく
  ユーザーがメインツールバーボタン用に選んだ製品アイコン（テキスト/インストーラーモード）
  を表示するようになります。
- Performance 設定タブに起動/編集用の 2 つのコントロールを追加：Cubism JVM ZGC トグル
  （`launcher.zgc`、デフォルトは「変更」節を参照）と「自動バックアップを無効化（クラッシュ
  復旧が弱まります）」トグル。後者は編集中にホストの定期自動バックアップタイマーを
  一時停止しつつ、設定済みの間隔と上限を維持します。観測されたホスト設定は最初の上書き前に
  プラグイン状態ベースラインへ記録され、フラグがオフのとき復元されるため、有効中の
  クラッシュでバックアップが無効のまま取り残されることはありません。手動の
  `backupNow`/`backupAfterSave` は影響を受けません。
- テクスチャアトラスの tile バウンディングボックス処理と出力キャッシュ再利用（ともに
  デフォルトで有効）。スクラッチと合成はアトラスページ全体ではなく変換後の tile バウンディ
  ングボックスに限定され、`CTextureAtlas.updateTexture` のコンテンツ署名ガードが同一署名の
  再出現時に保持済み記録出力の複製を供給します。レビュー済み実機フィクスチャでは、
  エディターの起動が約 100 秒から 15〜18 秒に、エクスポートの EDT 処理が約 127 秒から
  約 15 秒に短縮され、ページダイジェストはビット完全一致でした。レビュー済みターゲットは
  対応アーカイブを厳密に固定し、それ以外はすべてフェイルクローズします。
- ガード付きオプトインの性能実験：ネイティブテクスチャ準備、PNG アーカイブ再利用、
  Cubism 画像診断。すべてデフォルトでオフ。実機差分プローブが計測しますが、GPU/FPS や
  読み込み時間の高速化は主張しません。
- SDK をプラグイン開発者へ直接配布：すべてのフレームワークリリースに
  `turboism-sdk-<version>.jar` とその SHA-256 サイドカーが GitHub 限定の開発者アセットとして
  同梱され、`./gradlew publishToMavenLocal` でクリーンな `dev.turboism` 座標が得られ、
  `templates/plugin-template` はリリース済み SDK に対してビルドする独立プロジェクトです。
- Warp Deformer Alt Symmetry プラグインがバウンディングボックスの Alt セマンティクスを
  Warp Deformer コントロールポイントへ拡張：Alt ドラッグは垂直グリッド軸をまたいでミラー、
  Alt+Shift は水平軸をまたいでミラーします。キャンバス上部コントロールストリップの
  コントリビュートトグルが軸を武装し、ネイティブキャンバスヒントが武装状態を表示します。
  ミラー位置はモデル書き込みパスでコミットされ、対称移動がネイティブ Undo/Redo に
  加わります。プラグインは `turboism.cubism.model.read`、`turboism.cubism.model.write`、
  `turboism.ui.toolbar.contribute`、`turboism.ui.canvas.hint` を要求し、ネイティブミラーは
  検証済みホストフックがインストールされている場合のみバインドします。検証済み
  drag-tick フックはレビュー済みの Cubism 5.2.03、5.3.02、5.3.03 の厳密な成果物のみを
  許容し、ネイティブドラッグティック内でライブミラープレビューと単一のアンドゥ項目を
  提供します。未レビューのホストでは、同一結果のリリース時 AWT フォールバックが維持
  されます。
- `Action.of` と `MenuContribution.of` が単一点のコントリビューション登録を単純な
  `SimpleAction`/`SimpleMenuContribution` 値として構築するため、プラグインはアクションや
  メニュー項目ごとに匿名クラスを必要としなくなりました。

### 変更

- 通常 CI がすべての pull request と `main` への push で `devCheck` と完全な
  `checkCompletedCommit` スイートの両方を実行するようになり、表示依存テストには Xvfb を
  使用します。カバレッジガードはスキップ、フィルタ、ソフトフェイルされたゲートを拒否し、
  チャネルチェックは `main` に追随してルートビルド入力を含みます。
- Javadoc 存在チェックが行ベースのマッチングから JDK 17 コンパイラツリー API へ移行し、
  暗黙の public インターフェースメソッドと公開到達可能なネスト型をカバーします。解析や
  ツールの失敗は、不完全な結果へのサイレントフォールバックではなくチェック失敗になります。
- SDK、ランタイム、公式プラグイン全体の公開 API ドキュメントが、これまで未記載だった
  宣言を説明し、ライフサイクル、所有権、失敗、結果のセマンティクスを明確化しました。
- 組み込みコアプラグインはフレームワークシェル（`dev.turboism.shell`）としてランタイムに
  統合されました：プラグイン JAR としては配布されなくなり、予約済みの `turboism.core`
  アイデンティティは引き続き設定・タスク・ログを帰属し、プラグイン管理は引き続き削除不可の
  コア行として一覧表示し、プラグインロードレポートには表示されなくなります。共有
  トップメニュールートはルーティングキーとして「Turboism」を保持しますが、フレームワーク
  ローカライズ名を表示します。
- マネージド起動で Cubism がデフォルトで ZGC（`-XX:+UseZGC`）上で動作するようになりました：
  実機 A/B では G1 の秒スケールの停止（読み込み約 1.4 秒 → サブミリ秒、書き込み約 0.6 秒 →
  サブミリ秒）が解消し、定常 RSS が約 3 分の 1 削減されました。Performance → Cubism JVM
  トグルは次回起動時に適用され、明示的なオプトアウトのみを保存します。
- CSV パラメータ一括インポートが、書き込みごとのコミットではなく単一のオーサリング
  トランザクション内で実行されるようになりました。高負荷モデルの実機 A/B では、
  Update Parameter Structure の再構築が 37 回から 4 回（-89%）に、パラメータ書き込み中央値が
  4.71 ms から 1.19 ms（-75%）に、35 回書き込みバースト全体の EDT アロケーションが約
  99.8% 削減されました。
- ランタイムの読み取りは、すべてのアクセサを再投影する代わりに、SDK 型の監視シームを通じて
  バージョン付き読み取りまたはスコープキャプチャごとに 1 回だけホストを監視します。
  合成ベンチマークで読み取りパス時間約 29% 削減、呼び出しあたりアロケーション約
  41〜43% 削減を計測しています。
- Cubism 起動抑制（更新チェック、スプラッシュ、情報ダイアログのスキップ）が、新規
  インストールや `config.json` がまだない Turboism home での実行を含め、デフォルトで有効に
  なりました。`hooks.startup.skip*` フラグを `false` に設定するかセーフモードを有効にすると
  オプトアウトでき、スキーマ不正な設定は引き続きフェイルクローズします。Premain 診断は
  ランタイムログシンクがインストールされるまでバッファされ、`STARTUP_SUPPRESSION_*`
  許可コードがセッションログに届きます。
- リリースゲートがローカライゼーションを Cubism の全言語マトリックスに課すように
  なりました。Marketplace プラグインリスティングは宣言されたすべてのロケール
  （en、ja、ko、zh-Hans、zh-Hant）で `plugin.name`/`plugin.description` を持つ必要があり、
  レビュー済みの zh、ja、ko ノート翻訳が存在しないリリース候補は拒否されます。
  フレームワークメッセージカタログも公式プラグイン完全性ゲートに加わり、`checkIntegration`
  の一部としても実行されるようになりました。
- すべてのオプション `PluginContext` サービスアクセサが、getter から
  `UnsupportedOperationException` をスローする代わりに、そのサービスの `unavailable()`
  センチネルを返すようになり、各サービスは `isAvailable()` でプローブできます。センチネルは
  引き続きフェイルクローズします：ドメインが提供する場所（タスク送信、ホスト読み取り、
  ストレージ、ユーザーファイル、メッシュ編集、ホストダイアログ、スクリプト実行）では
  構造化された失敗を報告し、提供しない場所では使用時に安定した
  `UnsupportedOperationException` をスローします。`ScriptService#available` は
  `isAvailable` に改名され、プローブはどこでも同じ名前になりました。**プラグイン API の
  移行が必要な場合があります：** アクセサの `UnsupportedOperationException` を捕捉して
  いたプラグインは、代わりに `isAvailable()` を呼び出してください。
- 18 個のセマンティックイベント型が `dev.turboism.sdk.event.cubism` から
  `dev.turboism.sdk.cubism.event` へ移動しました。廃止されたパッケージは境界チェックと
  パッケージレイアウトチェックで拒否され、非推奨の形は戻せません。**プラグイン API の
  移行が必要な場合があります：** イベントの import を更新してください。
- インストール時ホストフックは `META-INF/turboism/hooks` で宣言され、手動配線ではなく
  エージェントがスキャンします：各 `HookContributor` が自身の許可をチェックし、
  `HookRegistry` を通じて install/bind/uninstall を転送するため、新しいフックはエージェント
  への編集ではなく、マニフェスト 1 行と contributor クラス 1 つで済みます。検証済み/
  フェイルクローズ許可、アトミックなレコード抽出、mesh-mirror premain/bind ライフサイクル、
  `installation=`/`cleanup=` レポート行は変更ありません。

### 修正

- アニメーションドキュメント、シーン、トラック、属性がオブジェクトグラフ全体でプラグイン
  権限、スコープ生存性、ドキュメント世代を強制するようになりました。キーフレームコピーは
  古いまたは外部のソースを拒否しつつ、同一プラグインが所有するアクティブビュー間の
  有効なコピーは保持します。
- アニメーション時間スケーリングが、ハンドルを単にシフトするのではなく、キーフレームと
  そのベジェコントロールハンドル時間に同じアフィン変換を適用するようになりました。
  小数のハンドル時間と大きな原点まわりの恒等スケーリングは精度を保持し、平行移動と
  コピーの動作は変更ありません。
- 保持される `cubismRead()` と Clip Mask コレクションサービスが、所有プラグインスコープの
  クローズ後にアクセスを拒否するようになりました。直接の PSD、clip-mask、texture-atlas、
  render、workspace、theme 読み取りパスを含み、他のプラグインを無効化しません。
- 物理エディターコントリビューションはプラグインスコープのクローズ時に自動解放されます。
  クローズ済みサービスは新しいコントリビューションを登録できず、繰り返しのクローズで
  後のコントリビューションを削除できません。
- アトラスキャッシュ再利用は、入力ダイジェストを計算できない場合（既存のピクセル予算を
  超える画像を含む）に元の再構築パスへフォールバックするようになりました。未知の
  ダイジェストは等しいと比較されず古いピクセルを返さなくなり、通常の未変更入力は
  再利用の対象のままです。
- `Motion3Validator` は `Version` とセグメント種別識別子をナローイングする前に厳密な
  数値をチェックし、小数値やオーバーフロー折り返し値を拒否しますが、それらのフィールドの
  極端な指数でスローしません。数学的に等価な有効な表現は引き続き受理されます。
- マネージド Graal インストールは、別のダウンロードを開始する前に、中断された初回
  インストールが残した検証済みで現行バージョンの孤立アクティベーションマーカーを調整する
  ようになり、完全なダウンロードとプローブの後に `GRAAL_RUNTIME_RECOVERY_REQUIRED` に
  なる事態を回避します。未知のマーカーと既存のロールバック保護は引き続き保護されます。
- 各 `DisposableScope` 登録は、登録ハンドルとスコープクリーンアップで共有される単一の
  リリースアクションを所有するようになり、繰り返しや並行クローズを含みます。等価リソースの
  個別登録は互いのクリーンアップ項目を削除しなくなりました。
- ホストアダプタアクセスを取る 3〜6 引数の `CorePluginContext` 便利コンストラクタが、
  null ポインタで失敗する代わりに、省略されたサービスをデフォルトパスで組み立てるように
  なりました。明示的サービスと厳密コンストラクタオーバーロードは既存契約を維持します。
- ランタイムテストフィクスチャが、一時ホストクラスを同名のクラスパスフィクスチャから
  分離し、ヘッドレスダイアログチェックを別のヘッドレス JVM で実行し、独自の設定
  ウィンドウスコープを閉じるようになり、関連するリンケージ失敗、モーダルハング、
  テスト間ウィンドウリークを解消しました。
- Cubism 5.3.03 エディターモデルの DRAFT マッピングメタデータが、参照する検証レコードと
  一致するようになりました。回帰チェックは厳密な件数、能力 ID、レコードダイジェストを
  検証し、損失のある数値変換を受理しません。これは新しいランタイムマッピングを有効に
  しません。
- 開いている 2 つのドキュメント間の切り替えで、新しくバインドされたモデルが直ちに公開
  されるようになりました。以前は前のモデルが公開されたままとなり、評価済み読み取りが
  新しいバインドアイデンティティの下で誤ったモデルを追跡し、固定する可能性がありました。
- エディターのバインドアイデンティティキャッシュが、セッションの存続期間中ずっと
  クローズ済みドキュメントグラフを保持しなくなりました（キャッシュされたアイデンティティは
  弱く保持され）、借用された Core モデルはそのバインドがなくなり次第解放されます。
- いくつかの無制限増加パスに上限が設定されました：オーバーレイボタンのサイドテーブルは
  FIFO で退避し、recent-preview メモリマップとポーラーの emit 重複排除マークは生存
  エントリに剪定され、非表示サブツリーはシーンパレットポールとパレットフィルタ
  コンポーネントウォーカーから剪定されます。
- リフレクション多用のホストパスが検証済みメンバー、コンストラクタ、所有者クラス、および
  永続的ミスをキャッシュし、mesh-mirror スキャンはディスパッチごとに 1 回だけインデックス
  し、ワークスペース `safeSegment` サニタイズパターンは事前コンパイルされ、パレット
  ツールバーは変更がないとき再レイアウトをスキップします。
- ログ編集が各パターンを厳密に必要とするバイトでゲートし、観測エントリあたり約 7 つの
  matcher アロケーションを削減しました。編集出力は変更ありません。
- フレームワークシェルに、システムクラスローダーフォールバック付きの agent jar 上の正式な
  `URLClassLoader` が追加され、残りのすべてのウィンドウ構築は `TurboismWindowFactory` を
  経由します。
- ホスト検証とプレビューパッケージングの修正：パラメータプラグイン JAR は古いバージョン名
  ではなく glob で特定され、検証プローブは宣言された i18n カタログを同梱し、ワークスペース
  バンドルは正規の agent jar からパッケージ化され、ランナーはローカルホスト転送と Windows
  結果行をサポートし、クローズフェーズ成果物が自動結果にカウントされ、プラグイン管理
  再起動フックがレビュー済みキュー目録に認められ、Cubism 5.3.03 厳密ホストが検証キューに
  加わりました。
- フックレジストリが、プロセス終了クローズパスと遅いエンロールが競合したときにハンドルを
  破損または二重クローズしなくなりました：クローズパスはスナップショットを走査して各
  エントリをアトミックにクレームし、パス途中でエンロールされたハンドルは半クローズ
  されずにエンロール状態を維持します。
- エージェントはブートクラスパス上でフックマニフェストを読み取るため、agent JAR 内に
  パッケージされたマニフェストはプロセスの作業ディレクトリに関係なく解決されます。

## [0.44.0] - 2026-09-11

### 追加

- Turboism がデプロイ済みリリース API（`api.turboism.dev/v1/releases/stable.json`）に対して
  安定版更新をチェックするようになりました。比較にはインストール済みパッケージに埋め込まれた
  権威あるビルド番号を使用するため、より低いビルドが更新として提示されることはなく、
  ビルド番号が同じでバージョンが異なる場合は更新ではなくアイデンティティ競合として扱われ
  ます。ビルド番号以前のインストールは引き続きバージョンのみで比較し、捏造された番号が
  割り当てられることはありません。
- 利用可能な更新は、Turboism ドッキングパネルの項目ではなく、ホストが自身の右下メッセージ
  に使うのと同じ面である描画領域上の Cubism ネイティブヒントとして提示されます。ヒントは
  キー付きで、より新しい提示ビルドが前のテキストを置き換え、更新が提示されなくなると
  すぐに消えます。クリックすると固定の第一者ダウンロードページを開きます。リリースフィード
  の URL が開かれたりインストールされたりすることはなく、インストーラーが自動で
  ダウンロード・実行されることもありません。
- 更新チェッカーは非ブロッキングで最大 24 時間に 1 回実行され、手動チェックは常に利用
  可能です。自動チェックは Startup 設定タブに独自の永続トグルを持ち、Cubism 自身の更新
  抑制から独立しています。
- プラグインは `UiHostCapabilityService.notifyCanvasHint`、`notifyDismissibleCanvasHint`、
  `showCanvasHintWhile` と、`CanvasHintNotification`、`CanvasHintHandle`、
  `CanvasHintPosition`、`ConditionalCanvasHint` を通じて、描画領域上に Cubism ネイティブ
  ヒントを表示できます。表示には新しい `turboism.ui.canvas.hint` 権限が必要です。この能力は
  検証済み 5.2.03、5.3.02、5.3.03 ホストルートを通じてバージョンルーティングされ、
  ルートを解決できないホストでは近似ではなく利用不可を報告します。
  [SDK v10 レビュー](sdk/api-contracts/sdk-api-v10-review.md)を参照。このリビジョンは
  純粋な追加であり、プラグイン移行は不要です。
- 公開リリースノートが、簡体字中国語と日本語に加えてレビュー済み韓国語テキストも持つ
  ようになりました。リリースドキュメントは `notesByLanguage` として公開し、英語は翻訳が
  欠けているときにウェブサイトが表示するフォールバックのままです。Nightly の見出しと警告は
  4 言語すべてに翻訳され、生のコミット件名は原言語のまま明示的にラベル付けされます。

### 変更

- レビュー済み SDK 厳密ベースラインが v10 になり、canvas-hint コミットに固定されました。
  このリビジョンは 42 件の API レコードを追加し、削除・変更はありません。v9 と v8 は
  すべてのリリースが実行する歴史的厳密監査のままです。
- WebDAV バックアッププラグインが `backup` から `webdav-backup` に改名されました：Gradle
  モジュールと Java パッケージは `webdav-backup`/`dev.turboism.plugin.webdavbackup`、
  インストーラー成果物は `plugins/webdav-backup.jar`（旧 `plugins/backup.jar`）、
  プラグイン id は `dev.turboism.plugin.webdav`（旧 `dev.turboism.plugin.backup`）、
  メニュー項目はローカライズされました。`backup/webdav.cfg` に保存されたエンドポイント
  設定は影響を受けません。
- レビュー済みマトリックス外の言語を名指す `release-notes/<version>.json` ファイルは、
  サイレントに捨てるのではなくリリースを失敗させるようになり、タイポで翻訳が欠けた
  リリースを出荷できなくなりました。

### 修正

- リリース API が GitHub 未到達時に、すべてのチャネルへ「unavailable」と答える代わりに、
  最後に検証されたリリーススナップショットを提供し続けるようになりました。スナップショットは
  最大 24 時間使用でき、1 チャネルの一時的失敗でそのチャネルの以前検証済みデータが
  捨てられることはなく、失敗したリフレッシュは以前のスナップショットを保持します。
  公開は引き続き API に即座に通知し、新しいスケジュールモニターが確認済みで重複排除された
  インシデントを報告します。
- Windows インストーラーが、プレインストール済み Java を必要とせずマネージド Graal
  ランタイムをプロビジョニングするようになりました：アーカイブを直接ダウンロードして
  検証し、スタンドアロンインストール用に完全なランタイム設定を初期化し、Graal ページは
  旧式の Java 前提条件を主張しなくなりました。プロビジョニングパスは Windows
  PowerShell 5.1 と 7 で検証済みです。
- 既存インストールのアップグレードで WebDAV プラグイン項目が 2 つ残らなくなりました。
  両インストーラーはマネージドアップグレード中に、組み込みプラグイン id で `plugins/`
  から改名前の古い JAR を削除するため（任意のファイル名をカバー）、古い id
  `dev.turboism.plugin.backup` は廃止/置き換え境界に加わりました：ランタイムはロードを
  拒否し、プラグイン管理は一覧せず、`config.json` の `disabledPlugins` も保持しません。
  改名前の WebDAV 設定ダイアログも、常に中国語を表示する代わりに、すべてのラベル、
  ボタン、ツールチップ、ステータスメッセージをローカライズします。

## [0.43.11] - 2026-09-11

### 追加

- インストーラーがホストロケールだけに頼るのではなく明示的な言語選択を提供するように
  なり、韓国語が英語、簡体字中国語、日本語に加わりました。NSIS ウィザードはウェルカム
  ページの前に標準言語ダイアログを表示し、ホスト言語に関係なくすべてのロケールを一覧
  します。IzPack インストーラーは `kor` 言語パック、そのライセンスリソース、モーダル
  言語パックセレクターを同梱します。選択したインストーラー言語はインストーラースコープに
  とどまり、`config.json` には書き込まれません。
- `GET /v1/downloads/<version>.json` がリリースごとのダウンロードリクエスト開始数を報告
  します。公式ミラー開始数は GitHub のバイナリ `download_count` に加算され、レスポンスは
  バイナリごとに 1 行の `assets` を持ち、名前、キー、SHA-256、official、GitHub、total
  値がリリース合計と整合します。チェックサムサイドカー、HEAD/304、失敗リクエスト、
  非ゼロ再開レンジ、verification プレフィックス付きトラフィックは除外され、未知のソースは
  捏造されたゼロではなく `null` のままです。
- Stable、Beta、Nightly リリースが、英語フォールバック付きのレビュー済み簡体字中国語・
  日本語ノート（`notesByLanguage`）を持つようになり、ウェブサイトはローカルで言語を
  選択します。ダイジェストが厳密な英語セクションと一致しなくなった翻訳は再利用されず
  拒否されます。Nightly は候補準備中に公開済み祖先ベースラインと実コミット件名を凍結する
  ため、後から着いたコミットはビルド済み候補の内容を変えられません。
- レビュー済み翻訳は、歴史的リリースを書き換えずに補強もできます：0.43.10 と
  0.43.10-0.nightly.3 は、厳密なリリース ID、ソースリビジョン、元の可視ボディに結び付け
  られた表示補足を受け取り、公開 Release ボディ、タグ、レシート、ファイル、ビルド番号は
  変更されません。
- フレームワークメッセージカタログが `verifyFrameworkCatalogs` により公式プラグインと
  同じロケールマトリックスに課されるようになりました。プラグインは不完全なカタログ
  セットで既に明確に失敗していましたが、フレームワークは `ResourceBundle` でクロームを
  解決し、カタログ欠落はサイレントに英語へ劣化していました。新ゲートは検証済みルート外の
  カタログを持つフレームワークモジュールも拒否します。

### 変更

- Beta と Nightly 候補は凍結ノートコンテキスト（`schemaVersion: 2`）を記録し、昇格は
  Stable ノートを厳密な `CHANGELOG.md` セクションとレビュー済み翻訳ダイジェストに
  結び付けます。チェックアウト後に `CHANGELOG.md`、`release-notes/`、ノートモジュールが
  変わった候補は、レビューされていないノートを公開する代わりにフェイルクローズします。
- Java アンインストーラーは `config.json` を保持するのがデフォルトになり、NSIS
  アンインストーラーと一致しました。プロパティなしのヘッドレス/コンソール実行も保持します。
- レビュー済み SDK v9 厳密アンカーがホストロケール修正へ移動し、SDK 契約はランチャーの
  DISPLAY ロケールではなく適用された言語を保持します。正規 API ダンプは不変で、
  `UiHostCapabilityService.hostLocale()` のデフォルトボディのバイトのみが移動し、
  v2〜v8 の歴史的アンカーは監査済みのままです。

### 修正

- プラグイン UI 言語が Cubism Editor の File → Environment Settings → General → Language
  で選ばれた言語に従うようになりました。ランチャーの `-Duser.language` はビルドの言語版を
  選ぶだけで実行時に変化しないため、ホスト言語として扱われなくなりました。Cubism が保存済み
  設定をこのランタイムのアタッチ後にプロセスデフォルトロケールへ適用するため、有効
  ロケールは検証済みホストが ACTIVE になった時点で再解決されます。明示的な
  `-Dturboism.locale` や `config.json` ロケールは引き続きホストに優先します。
- フレームワーク自身の `ResourceBundle` カタログが完全な zh-Hans/zh-Hant/en/ja/ko
  マトリックスを持つようになりました。`dev.turboism.ui.panel` には `messages_en.properties`
  と `messages_zh_Hans.properties` がなく、簡体字中国語ホストはサイレントに旧来の
  スクリプトなし `messages_zh.properties` へフォールスルーしていました。そのカタログは
  オプションの互換エイリアスとして保持されますが、スクリプト付きカタログの代用には
  なりません。
- ツールバーアイコンはプラグインが同梱する表示スケールバリアント（125/150/175/200%）で
  読み込まれ、単一のマルチ解像度アイコンとして解決されるため、インストーラーエントリは
  高 DPI ディスプレイで単一の非スケールビットマップから描画されなくなりました。
- Java アンインストーラー確認ダイアログの韓国語ブランチが、英語へのフォールバックではなく
  ローカライズされ、4 つの README テンプレートはアンインストーラーのデフォルト保持型
  `config.json` チェックボックスを説明するようになりました（デフォルト削除型ではなく）。

## [0.43.10] - 2026-09-09

### 追加

- 明示的スケール契約とガード付きネイティブパッキング統合による、現在ページの
  テクスチャアトラスパッキング。
- 安定ナビゲーションと設定可能なコアツールバーアイコンを持つ、キャプチャ済みセマンティック
  履歴タイムライン。
- 独立リリース API、GitHub リリース同期、検証済みストリーミングミラー、グローバルに
  割り当てられた製品ビルドアイデンティティ。

### 変更

- レビュー済み SDK v9 厳密リリースベースラインを採用し、v8 を歴史的監査へ降格。アンカーは
  表示フィールドを 1 つ追加：`CubismOperationEvent` にオプションの `label` が追加され、
  観測された Cubism Editor ネイティブ編集がローカライズ可能なネイティブ編集名を持てる
  ようになります。コンポーネントは `subjectId` の後に追加され、明示的にアイデンティティ
  ではありません。**プラグイン API の移行が必要な場合があります：** `CubismOperationEvent`
  を直接構築するプラグインは新しい第 5 コンポーネントを渡す必要があります。
  [SDK v9 レビュー](sdk/api-contracts/sdk-api-v9-review.md)を参照。
- レビュー済み SDK v8 厳密リリースベースラインを採用し、v7 を歴史的監査へ降格。アンカーは
  v7 ゲートが記録しなかったキャプチャ済みセマンティックタイムラインと UI 面、およびネイティブ
  エディター編集用の 4 つの新しい `CubismOperation` アイデンティティ
  （`SET_HIERARCHY_PARENT`、`DETACH_HIERARCHY_PARENT`、`MOVE_DRAWABLE`、
  `SET_DRAWABLE_COLOR`。既存定数の序数が動かないよう追加）を捕捉します。**プラグイン API の
  移行が必要な場合があります：** `HistoryEntry`、`RuntimeSettings`、`PanelView.Toggle`
  コンストラクタが変更されました。[SDK v8 レビュー](sdk/api-contracts/sdk-api-v8-review.md)
  を参照。
- 製品リリースを読み取り専用候補ビルドと明示的で保護された GitHub 昇格に分割。失敗した
  候補試行は意図したバージョンを再利用し、昇格のみが公式の注釈付きタグを作成し、再ビルド
  せずに検証済みバイトを公開します。
- レビュー済み SDK v8 現在ページテクスチャレイアウト契約を採用。
- 4 言語のプロジェクト・インストールドキュメントと、タスク内完結のローカルホスト検証
  監督を追加。

### 修正

- マルチバージョン Scene パレットブリッジを強化し、厳密な Cubism 5.3.03 ルーティングを
  追加し、統一パレットクリーンアップを維持。
- コアツールバー画像をホストのホームアイコンサイズに一致。
- ホスト検証の環境処理と不正な結果の拒否を強化。

## [0.43.9] - 2026-09-06

このリリースは未公開の 0.43.4〜0.43.8 候補を置き換えます。

### 追加

- 同期オーサリングトランザクション、グループ化 Glue 書き込み、安定した履歴エントリ/
  トランザクションアイデンティティ、拡張された型付き MCP 読み書き操作、JAR ファイルからの
  直接プラグインインストール、復元されたバウンディングボックスオーバーレイコントロール。

### 変更

- レビュー済み SDK v7 厳密リリースベースラインを採用し、v2〜v6 の歴史的ベースラインを
  維持。**プラグイン API の移行が必要な場合があります：** `McpHttpConnection` は
  endpoint/protocol のみを受け取り `authorization()` を公開しなくなりました。
  `HistoryEntry` は追加のアイデンティティフィールドを持ち、例外列挙の序数が変更されました。
  [SDK v7 レビュー](sdk/api-contracts/sdk-api-v7-review.md)を参照。
- パレットツールバー/フィルターコントリビューションを統一し、重複するモデル・履歴スキャンを
  削減。
- ローカルループバック MCP サーバーがベアラー認証なしで動作するようになりました。
- リリースパッケージからマネージド fx ランタイムバイトと開発専用 Turboism with fx
  プラグインを除外。

### 修正

- スナップショット競合と非同期プラグイン無効化の回帰テストを安定化し、バウンディング
  ボックスドラフトメタデータを修正し、ネイティブ Java インストーラー Full ペイロード
  テストを Windows ポリシーカバレッジから分離（本番検証は弱めず）。
- ペイロード変更前のインストーラー設定検証、パラメータ一括転送行レイアウト修正、SDK
  インターフェースプロキシカバレッジ、Cubism 5.3.03 テクスチャアトラス自動レイアウト
  フック選択、MCP/ランタイムトランザクションとライフサイクル修正を含む。

## [0.43.8] - 2026-09-06

このリリースは未公開の 0.43.4〜0.43.7 候補を置き換えます。

### 追加

- 同期オーサリングトランザクション、グループ化 Glue 書き込み、拡張された型付き MCP
  読み書き操作、JAR ファイルからの直接プラグインインストール、復元されたバウンディング
  ボックスオーバーレイコントロール。

### 変更

- パレットツールバー/フィルターコントリビューションを統一し、重複するモデル・履歴スキャンを
  削減。
- ローカルループバック MCP サーバーがベアラー認証なしで動作するようになりました。
- リリースパッケージからマネージド fx ランタイムバイトと開発専用 Turboism with fx
  プラグインを除外。

### 修正

- リリース検証を修正：決定論的なスナップショット競合と非同期プラグイン無効化テスト、
  スキーマ準拠のバウンディングボックスドラフトメタデータ、別個の Windows ポリシー
  カバレッジを持つネイティブ OS Java インストーラー Full ペイロードテスト。これらの検証
  修正は本番検証、ライフサイクル動作、ホストセレクターを変更していません。
- ペイロード変更前のインストーラー設定検証、パラメータ一括転送行レイアウト修正、SDK
  インターフェースプロキシカバレッジ、Cubism 5.3.03 テクスチャアトラス自動レイアウト
  フック選択、MCP/ランタイムトランザクションとライフサイクル修正を含む。

## [0.43.7] - 2026-09-06

このリリースは未公開の 0.43.4〜0.43.6 候補を置き換えます。

### 追加

- 同期オーサリングトランザクション、グループ化 Glue 書き込み、拡張された型付き MCP
  読み書き操作、JAR ファイルからの直接プラグインインストール、復元されたバウンディング
  ボックスオーバーレイコントロール。

### 変更

- パレットツールバー/フィルターコントリビューションを統一し、重複するモデル・履歴スキャンを
  削減。
- ローカルループバック MCP サーバーがベアラー認証なしで動作するようになりました。
- リリースパッケージからマネージド fx ランタイムバイトと開発専用 Turboism with fx
  プラグインを除外。

### 修正

- Cubism 5.3.03 バウンディングボックスドラフトマッピングメタデータを既存スキーマに
  適合させました。セレクターと検証レコードは変更なく、ドラフトは未検証のままです。
- スナップショット競合と非同期プラグイン無効化の回帰テストを、本番検証やライフサイクル
  動作を変えずに決定論的にしました。
- ペイロード変更前のインストーラー設定検証、パラメータ一括転送行レイアウト修正、SDK
  インターフェースプロキシカバレッジ、Cubism 5.3.03 テクスチャアトラス自動レイアウト
  フック選択、MCP/ランタイムトランザクションとライフサイクル修正を含む。

## [0.43.6] - 2026-09-06

このリリースは未公開の 0.43.4 と 0.43.5 候補を置き換えます。

### 追加

- 同期オーサリングトランザクションスコープ、グループ化 Glue 書き込み、拡張された型付き
  MCP 読み書き操作、JAR ファイルからの直接プラグインインストール、復元されたバウンディング
  ボックスオーバーレイコントロール。

### 変更

- パレットツールバーとフィルターコントリビューションを本番ホストライフサイクル処理付きで
  統一し、重複するモデル・履歴スキャンを削減。
- ローカルループバック MCP サーバーがベアラー認証なしで動作するようになりました。
- リリースパッケージにマネージド fx ランタイムバイトや開発専用 Turboism with fx プラグインが
  含まれなくなりました。

### 修正

- リリース回帰カバレッジを安定化：スナップショットコピー競合は変更された・保持された
  タイムスタンプを明示的にカバーし、繰り返しプラグイン無効化テストはコールバック開始では
  なく終端ライフサイクル完了を待機します。これらのテスト修正は本番検証とライフサイクル
  動作を変更しません。
- ペイロード変更前のインストーラー設定検証、パラメータ一括転送行レイアウト修正、SDK
  インターフェースプロキシカバレッジ、Cubism 5.3.03 テクスチャアトラス自動レイアウト
  フック選択、MCP/ランタイムトランザクションとライフサイクル修正を含む。

## [0.43.5] - 2026-09-06

このリリースは未公開の 0.43.4 候補を置き換え、その変更を下記に含みます。

### 追加

- 同期オーサリングトランザクションスコープ、グループ化 Glue 書き込み、拡張された型付き
  MCP 読み書き操作、JAR ファイルからの直接プラグインインストール、復元されたバウンディング
  ボックスオーバーレイコントロールを追加。

### 変更

- パレットツールバーとフィルターコントリビューションを本番ホストライフサイクル処理付きで
  統一し、重複するモデル・履歴スキャンを削減。
- ローカルループバック MCP サーバーをベアラー認証なしで動作するよう変更。
- すべてのリリースパッケージからマネージド fx ランタイムバイトと開発専用 Turboism with fx
  プラグインを削除。

### 修正

- スナップショットコピー ABA 回帰テストをファイルシステムタイムスタンプ解像度から独立させ、
  観測可能なファイル変更とタイムスタンプ不変の破損スナップショットの両方をカバーし、
  本番検証を弱めませんでした。
- ペイロード変更前のインストーラー設定検証、パラメータ一括転送行レイアウト修正、SDK
  インターフェースプロキシカバレッジ、Cubism 5.3.03 テクスチャアトラス自動レイアウト
  フック選択、MCP/ランタイムトランザクションとライフサイクル修正を含む。

## [0.43.4] - 2026-09-06

### 追加

- グループ化 Glue 書き込みを含む同期オーサリングトランザクションスコープと、拡張された
  型付き MCP 読み書き操作を追加。
- JAR ファイルからの直接プラグインインストールを追加し、バウンディングボックスオーバーレイ
  コントロールを復元。

### 変更

- パレットツールバーとフィルターコントリビューションを本番ホストライフサイクル処理付きで
  統一。
- ローカルループバック MCP サーバーをベアラー認証なしで動作するよう変更。
- ランタイムホットパスでの重複するモデル・履歴スキャンを削減。

### 修正

- Windows と Java インストーラーがペイロード変更前に `config.json` を検証または移行し、
  整数スキーマトークンとランタイム有効な v1 値を要求し、無関係な設定を上書きせずに
  アップグレードプラグイン選択を適用するようにしました。
- すべてのリリースチャネルからマネージド fx ランタイムペイロードを削除し、開発専用
  Turboism with fx プラグインがどのリリースパッケージにも入らないことを文書化。オプションの
  Windows fx リゾルバテストはプライベートフィクスチャパスがない場合に安全にスキップする
  ようにしました。
- 重なり合うパラメータ一括転送バインド行、SDK インターフェースプロキシカバレッジ、
  Cubism 5.3.03 テクスチャアトラス自動レイアウトフック選択を修正。
- トランザクション結果とライフサイクル処理に関する MCP とランタイムのレビュー指摘に対応。

## [0.43.3] - 2026-09-03

### 追加

- Full インストーラーとアーカイブにマネージド Windows x64 fx v0.0.5 製品ペイロードを追加。
  起動前に厳密なサイズと SHA-256 で検証され、上流が Windows 修復アーカイブを提供しないため
  Windows 製品の修復や再インストールで復元されます。
- Core About ウィンドウの Turboism タイトル下に、ローカライズされた “For you, a bouquet”
  献辞を追加。
- リポジトリルートと公開インストーラーに、簡体字中国語が正文の完全な Turboism エンド
  ユーザー実行宣言 v2.0 を追加。プロジェクトアイデンティティ、合法的 Cubism 認可、
  ユーザーコンテンツバックアップ、現状のままの動作について 4 つの個別必須確認があります。
- 設定、サービス、ホストアダプタ、プラグイン読み込み、最終レポートの簡潔な起動フェーズ
  所要時間診断を追加。
- Turboism MCP Connection ウィンドウを追加。現在のローカル MCP アドレス、そのベアラー
  トークン、明示的コピーアクション、およびベアラー値や MCP セッション識別子を記録しない
  有界なプロセスローカル接続・リクエスト履歴を表示します。
- 安定したデフォルト MCP ポート `43123` を追加（`turboism.mcp.port=0` は引き続きエフェメラル
  ポートを選択）。検証済みの Claude Code、Visual Studio Code、Codex CLI 設定例を同梱。
- **Turboism → fx Settings** 直通メニュー項目を追加し、MCP や ACP 接続がなくてもランタイム
  パス、fx 所有シェル、プロバイダー設定へ到達可能にしました。
- 保存済み fx プロバイダープロファイルをモーダルの追加・編集・削除・選択ダイアログ付きで
  追加：fx 所有の Vercel、Codex、Grok ビルトインは厳密な fx ログインコマンドを起動し、
  カスタム OpenAI 互換またはセルフホストエンドポイントは Turboism 所有のループバック
  アダプタが提供します。
- カスタムプロバイダープロファイル向けに、ベストエフォートの `/v1/models` 検出とモーダル
  手動モデル ID ダイアログを追加。
- カスタムプロバイダー API キーの永続ストレージを追加し、キー入力は 1 回だけ：成功する
  場合は Windows DPAPI で現在ユーザー向けに保護し、それ以外はプラグイン自身の設定
  ディレクトリの `auth.json` に書き込みます。
- 決定論的な Windows インストーラーペイロード処理を追加。SHA-256 で変更のない JAR と fx
  製品ペイロードをスキップし、空の完了ページを表示しなくなりました。
- PSD Clip Mask Import の進行報告を追加し、インポート実行中の再入を防止。
- 永続的なインストーラーランチャーとマネージドサブプロセス診断を追加し、MCP パラメータ/
  モデル書き込み結果、明示的バインド存在、コミット済み作成を再試行安全かつランタイム診断へ
  相関させました。

### 変更

- Core About ウィンドウが権威ある Gradle リリースバージョンから生成されたフレームワーク
  バージョンを表示するようにしました。
- リリースプラグイン許可リストを修正し、History Panel と PSD Clip Mask Import の
  ビジネスプラグインを公開しつつ、開発シェル、デモ、レガシープレースホルダーを公開
  インストーラーとアーカイブから除外。
- Windows インストーラーを拡大し、手動コンフィギュレーターをリサイズ・最大化可能に。
  インストールは厳密に対応する Cubism Editor インストール（5.2.03、5.3.02、5.3.03）を
  検出し、見つかった互換インストールをすべて選択し、選択された Turboism ショートカットと
  ハッシュガード付き公式 BAT コントロールを、コンフィギュレーターを開かずヘッドレスに
  適用します。
- オプションのマネージド GraalVM インストール失敗やキャンセルを、残りの Turboism
  インストールを中断せず可視化・記録するようにしました。
- 通常の下部ステータス通知を最新メッセージスロットとして定義し、呼び出し元プラグインの
  スコープ付き Turboism ロガーを通じてすべてのステータス呼び出しを記録。コンパクトな
  常駐メトリクスは独立したキー付きスロットを保持します。
- すべての公式 UI Theme ロケールで、テーマ適用後は正しく描画されるよう Cubism Editor の
  再起動が必要であることを明確化。
- Windows fx 候補は Turboism の厳密な認証済み数値ループバック HTTP MCP サーバーのみを
  許容し、公式 Linux/macOS fx アセットとの永続セッション、ネイティブツール、汎用
  ネットワーク、プロセス、永続化の同等性は主張しないことを文書化。
- Clip Mask Viewer がローカライズされた読み込み状態を即座に表示し、デタッチされた関係
  インデックス、カウント、分析、グラフ投影を Cubism ホストスレッドから移動。キャンセルと
  古い結果ガード付き。
- fx v0.0.5 には Claude サブスクリプションログインがなく、Claude Pro/Max や Claude Code
  サブスクリプションは Anthropic API 認証情報ではないため、そのようなプロバイダー
  プロファイルは提供されないことを文書化。カスタムアダプタは OpenAI Chat Completions のみを
  実装します。
- fx の Gateway 推論レベルは推測された `reasoning_effort` に翻訳されるのではなく、意図的に
  OpenAI 互換エンドポイントへ転送されないことを文書化。
- fx Agent ウィンドウがプロバイダー、モデル、互換性の選択なしで自動接続するように。
  プロバイダー/モデル設定の欠落はユーザーがプロンプトを送信したときのみ報告されます。
- カスタムプロバイダーのデフォルトモデルをオプションにし、**Use** が選択プロファイルへ
  即座に再接続するようにしました。

### 修正

- MCP ツール書き込みがホストモデル操作と競合する問題を修正。モデルオブジェクト作成を
  Cubism ランタイムと一致させ、ランタイム診断リソースを接続状態から分離しました。
- MCP 書き込みがリトライを生き残れない、パラメータバインドとコミット済み作成が誤報告
  される、無効なランタイムリクエストが安全でない形で記録される問題を修正。書き込み警告は
  ランタイム診断と相関され、適用スキーマはバインドランタイムと一致します。
- PSD Clip Mask Import が名前空間をまたいでモデル id を比較し、異なるシーンの等しい id で
  誤発火し得た問題を修正。id 比較は単一名前空間内にとどまり、インポートは再入なしで進行を
  報告します。
- ペイロード SHA-256 処理後に Windows インストーラー完了ページが空になる問題を修正し、
  インストーラーランチャー診断がラベル付き stdout/stderr を確実にドレインするように
  しました。
- Windows アンインストーラーの設定保持チェックボックスが外側ウィザードウィンドウに
  接続されていた問題を修正。確実な操作を妨げ、確認ページを鈍くする可能性がありました。
- アンインストールオプションを明確でデフォルト有効の「Keep config.json」動作へ変更。
  ユーザーがチェックを外した場合のみ設定が削除されます。
- 厳密バージョン SDK 許可を維持しつつ、Cubism Editor 5.2.03 での History スナップショット
  可用性を復元。
- Java が利用可能な ACL ビューまたは既存のユーザー単位 Windows パスと再解析チェックを
  公開する場合に Windows MCP 起動を復元。ベアラー値、エンドポイント、プライベート接続
  ファイルパスは記録しません。
- 永続的なコンフィギュレーターとマネージド GraalVM サブプロセス診断（ラベル付き stdout/
  stderr の並行ドレインを含む）を追加し、失敗した BAT 統合とオプションランタイム設定を
  対処可能にしました。
- プラグイン有効化が空のプラグインごとの config、data、cache ディレクトリを作成しない
  ようにしました。ストレージは最初の実操作が必要とするディレクトリのみを作成し、プラグイン
  ログは共有ランタイムログを使い、廃止された `palette-filter-attach.tsv` 診断は生成され
  ません。
- 未サポートのリフレクションによるキャンバス外再描画の試行を削除。テーマ変更は Cubism
  Editor 5.2.03 と 5.3.02 で確認された信頼できる再起動必須動作を使います。
- fx Settings ウィンドウが fx シェルを開く前に確立済み接続を要求し、新規インストールで
  プロバイダーとモデル設定に到達できなかった問題を修正。
- カスタムエンドポイントアダプタが、fx がすべてのリクエストで送る reasoning フィールドを
  拒否し、`ai-language-model-id` リクエストヘッダーを無視し、認証なしセルフホスト
  エンドポイントに API キーを要求していた問題を修正。
- カスタムプロファイルがユーザーの通常 fx home から無関係な Codex や Grok 選択を継承する
  問題を修正。適応接続はプラグイン所有の Gateway 専用 fx home とオプションの直接 `--model`
  引数で起動します。
- Turboism が利用可能なプロバイダーやモデルが選択されていないことを報告する前にプロンプト
  テキストが消去される問題を修正。
- ACP MCP サーバーを拒否していた制限付き Windows fx ペイロードを、Turboism の厳密な認証済み
  数値ループバック HTTP MCP サーバーに限定した独立再現可能ビルドで置き換えました。
- マネージド Windows 起動がパスを含む Turboism JVM オプションを独立した `cmd.exe`
  コマンドとして再解析していた問題を修正。オプションは一時 Cubism BAT 内で引用符付き引数
  として挿入され、継承された Java オプション変数と古い Turboism 統合ブロックは子プロセス
  から除外されます。

## [0.43.2] - 2026-08-29

### 修正

- Cubism バンドル JVM がオプションの `java.net.http` モジュールを公開しない場合に、
  Turboism ランタイム起動が Core プラグイン UI 登録前に中断される問題を修正。マネージド
  GraalVM コントロールは、メニュー、ツールバー項目、パネルを無効化せずにフェイル
  クローズするようになりました。
- マネージド GraalVM の全ダウンロード期限を 20 分から 4 時間に延長し、遅いが継続的に
  進行する Windows ダウンロードが早まって終了しないようにしました。

## [0.43.1] - 2026-08-29

### 変更

- Windows コンフィギュレーターの独立ショートカットとショートカット引き継ぎモードを
  明確化。両モードで公式 Cubism BAT ファイルは変更されず、生成されるマネージド `.lnk`
  ファイル名に空白を含めず、Turboism ディレクトリを開くインストール完了オプションを
  追加しました。
- `logs/installer/managed-graal-install.log` に永続的なマネージド GraalVM インストール
  進行と診断を追加。

### 修正

- Windows PowerShell 起動とマネージド GraalVM ヘルパーが、大文字小文字を区別しない
  `$home` 変数と PowerShell の読み取り専用 `$HOME` 自動変数の衝突で失敗していた問題を修正。
- OpenJDK が Windows で `BasicFileAttributes.fileKey()` に null を返すため、マネージド
  GraalVM インストールが通常の Windows ファイルとディレクトリを拒否していた問題を修正。
  Windows は利用できないキーを要求せず、ファイル種別、サイズ、再解析ポイント状態を
  再検証するようになりました。

## [0.43.0] - 2026-08-28

### 追加

- Cubism Editor 5.3.03 向けの厳密バージョンランタイム、SDK 可用性、オーサリング、履歴、
  ライフサイクル、テクスチャアトラス対応。
- Turboism with fx。ACP 統合、永続セッション復旧、対応する Linux と macOS Java
  インストーラーパッケージ向けのレビュー済みマネージド fx ランタイムペイロードを含む。

### 変更

- GitHub README を拡張し、対応 Windows ホストプラットフォーム、厳密な Cubism Editor
  バージョン、インストール選択肢、現在の機能、検証ガイダンスを追加。
- すべての GitHub Release が `CHANGELOG.md` の一致バージョンセクションからノートを取得する
  回帰チェックを追加。
- 再利用可能なテストサポートとクロスモジュール統合テストを `testing/` にまとめ、
  レビュー済み SDK API 契約を `sdk/` ドメイン下へ移動。
- 公開 Cubism 互換性契約を、無視されるローカル Cubism 参照とホスト証拠から分離し、
  生成された参照レポートを `build/reports/` 下へ移動。
- リポジトリ衛生を強化し、ローカル参照、調査、AI レビュー証拠、生成レポート、検証出力
  パスの強制追加を拒否。
- Windows セーフと Java インストーラーのペイロードステージングを分割し、マネージド
  ネイティブ fx ランタイムをレビュー済み Linux と macOS Java パッケージに限定。
- 決定論的リリース計画、厳密な候補・ペイロード検証、不変の 8 アセットフレームワーク
  契約、協調した Plugin Directory と Updates 公開シーケンスを追加。

### 修正

- レガシー SDK 履歴実装を維持しつつ、古いドキュメントバインドを拒否し、ホストオブジェクト
  グラフを漏らさずにネイティブバインドを保持。
- パラメータグループとバインドバッチアクセスを線形スケールにし、すべてのネイティブ
  テクスチャアトラスエントリを保持し、アトラスリゾルバとビューを単一ホスト世代にバインド。
- マネージドランタイムライフサイクル、ACP 永続セッション再生、インストーラープラットフォーム
  ポリシー、ローカル JSON 解析（ASCII のみの JSON 数字と Unicode エスケープを含む）を強化。
- 所有者のみ権限や保護された Windows DACL 証明を確立できない場合に MCP ベアラー公開を
  フェイルクローズに。
- 公開前に厳密なプラグイン名簿、Windows/Java ペイロード分離、すべてのリリース検証器
  呼び出しサイトを検証。

### 既知の制限

- Windows パッケージにマネージドネイティブ fx ランタイムは含まれません。ネイティブの
  保護 DACL と再解析セーフなベアラーファイル公開機構が利用可能になるまで、MCP ベースの
  Turboism with fx は Windows で利用できません。
- ネイティブ Unix プロセスグループと Windows Job Object 封じ込めが実装されるまで、
  マネージド子プロセスクリーンアップはベストエフォートのままです。
- 公開バイナリはコード署名・公証されていません。ダウンロード後は付属の SHA-256
  サイドカーで検証してください。

## [0.42.0] - 2026-08-25

### 追加

- Live2D Cubism Editor 向け Java 17 エージェントランタイムと公開プラグイン SDK。
- Cubism Editor 5.2.03 と 5.3.02 向け厳密バージョンランタイムアダプタ。
- Windows NSIS インストーラー、Lite と Full ZIP ディストリビューション、クロスプラットフォーム
  IzPack インストーラー。
- 公式第一者プラグインバンドル。プラグインライフサイクル、権限、設定、ローカライゼーション、
  タスク、イベント、アクション、メニュー、ツールバー、ワークスペース、Cubism 統合サービスを
  含む。
- 決定論的リリースメタデータを持つプラグインパッケージ検査と Plugin Directory 統合。
- すべての公開インストーラーとアーカイブの SHA-256 サイドカー。

### 変更

- 公開 SDK ガバナンスを、厳密な Cubism Editor 可用性注釈付きの単一リリース API 層に統合。
- ランタイムイベント配信を統一し、プラグイン有効化、置き換え、解体、失敗分離を強化。
- リリースパッケージングを単一共用ステージドペイロードと単一レビュー済みプラグイン許可
  リストで使用するようにしました。

### 修正

- 実機フック、公開イベント検証、プラグインシャットダウン、バックアップ継続フェンシング、
  最近プレビューのホバーサムネイルを安定化。
- パッケージ検査、設定マージ、パス処理、レポート編集、サプライチェーン検証を強化。

### 既知の制限

- Windows が主要な Cubism ホストプラットフォームです。
- Java インストーラーは macOS と Linux で利用できますが、macOS Cubism ホスト対応は主張
  されず、Linux Cubism ホスティングは未対応です。
- このリリースの公開バイナリはコード署名・公証されていません。インストール前に付属の
  SHA-256 サイドカーで検証してください。

[Unreleased]: https://github.com/Turboism/Turboism/compare/v0.44.0...HEAD
[0.44.0]: https://github.com/Turboism/Turboism/releases/tag/v0.44.0
[0.43.11]: https://github.com/Turboism/Turboism/releases/tag/v0.43.11
[0.43.10]: https://github.com/Turboism/Turboism/releases/tag/v0.43.10
[0.43.9]: https://github.com/Turboism/Turboism/releases/tag/v0.43.9
[0.43.8]: https://github.com/Turboism/Turboism/releases/tag/v0.43.8
[0.43.7]: https://github.com/Turboism/Turboism/releases/tag/v0.43.7
[0.43.6]: https://github.com/Turboism/Turboism/releases/tag/v0.43.6
[0.43.5]: https://github.com/Turboism/Turboism/releases/tag/v0.43.5
[0.43.4]: https://github.com/Turboism/Turboism/releases/tag/v0.43.4
[0.43.3]: https://github.com/Turboism/Turboism/releases/tag/v0.43.3
[0.43.2]: https://github.com/Turboism/Turboism/releases/tag/v0.43.2
[0.43.1]: https://github.com/Turboism/Turboism/releases/tag/v0.43.1
[0.43.0]: https://github.com/Turboism/Turboism/releases/tag/v0.43.0
[0.42.0]: https://github.com/Turboism/Turboism/releases/tag/v0.42.0
