# Used only when libs/ does not contain a turboism-sdk-*.jar.
# Point this at the version you published with `./gradlew :sdk:publishToMavenLocal`
# inside the Turboism repository (development builds end with -SNAPSHOT).
turboismSdkVersion=0.44.0-SNAPSHOT
