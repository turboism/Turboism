package dev.turboism.sdk.cubism;

/** Semantic kind of a file-backed entry owned by the current Cubism project. */
public enum ProjectContentKind {
    MODEL,
    ANIMATION,
    IMAGE,
    GAME_DATA,
    OTHER
}
