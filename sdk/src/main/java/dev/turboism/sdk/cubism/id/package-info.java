/**
 * Turboism-owned opaque identities for Cubism and Editor objects.
 *
 * <p>Identity value types are shared across read, query, transaction, and
 * event interfaces. Feature packages must not define duplicate ID types.</p>
 */
package dev.turboism.sdk.cubism.id;
