[EN / English](CHANGELOG.md) · [ZH / 简体中文](CHANGELOG_zh.md) · [JP / 日本語](CHANGELOG_ja.md) · [KR / 한국어](CHANGELOG_ko.md)

# 변경 기록

Turboism의 모든 주요 변경 사항은 이 파일에 기록됩니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며, 버전은 시맨틱 버저닝을 따릅니다.

## [Unreleased]

### 추가

- SDK와 런타임에 애니메이션 워크스페이스 지원: 플러그인이 애니메이션 문서, 프로젝트
  타임라인, 트랙, 속성, 키프레임을 열거하고, 장면을 활성화·이름 변경하고, 재생 위치를
  이동하고, 일괄 키프레임 편집과 커브 타입을 적용하고, 평가된 값을 기록/베이크할 수
  있습니다. 순수 SDK의 `Motion3Validator`가 motion3 데이터의 구조적 문제를 보고합니다.
  객체 모델은 `animation-timeline-host-probe`를 통해 Cubism 5.2.03, 5.3.02, 5.3.03에서
  실기기 검증되었습니다.
- `PluginContext.availableServices()`와 `PluginService` 열거형이 런타임이 실제로 설치한
  선택적 컨텍스트 서비스를 보고하므로, 플러그인이 더 이상 getter를 탐색하거나
  `unavailable()` 센티널을 추측할 필요가 없습니다. 기본값은 빈 집합으로 페일 클로즈됩니다.
- `TurboismWindowFactory.installWindowIcon`이 프로세스 전체 창 아이콘 재정의를 설치하여,
  모든 플러그인 소유 창이 번들 기본값 대신 사용자가 메인 툴바 버튼용으로 선택한 제품
  아이콘(텍스트 대 설치 프로그램 모드)을 표시합니다.
- Performance 설정 탭에 두 개의 시작/편집 컨트롤이 추가되었습니다: Cubism JVM ZGC 토글
  (`launcher.zgc`, 기본값은 "변경" 참조)과 "자동 백업 비활성화(크래시 복구 약화)" 토글.
  후자는 편집 중 호스트의 주기적 자동 백업 타이머를 일시 중지하면서 구성된 간격과 상한을
  유지합니다. 관찰된 호스트 설정은 첫 재정의 전에 플러그인 상태 기준선에 기록되고 플래그가
  꺼지면 복원되므로, 활성 상태에서 크래시가 나도 백업이 비활성화된 채로 남지 않습니다.
  수동 `backupNow`/`backupAfterSave`는 영향을 받지 않습니다.
- 텍스처 아틀라스 타일 바운딩 박스 처리와 출력 캐시 재사용(둘 다 기본 활성). 스크래치와
  합성은 전체 아틀라스 페이지가 아니라 변환된 타일 바운딩 박스로 제한되며,
  `CTextureAtlas.updateTexture`의 콘텐츠 서명 가드가 동일 서명이 다시 나타날 때 보존된
  기록 출력의 복제본을 제공합니다. 검토된 실기기 픽스처에서 에디터 열기가 약 100초에서
  15~18초로,내보내기 EDT 작업이 약 127초에서 약 15초로 단축되었고 페이지 다이제스트는
  비트 단위로 동일했습니다. 검토된 대상은 지원되는 아카이브를 정확히 고정하고 나머지는
  모두 페일 클로즈됩니다.
- 가드된 옵트인 성능 실험: 네이티브 텍스처 준비, PNG 아카이브 재사용, Cubism 이미지
  진단. 모두 기본 비활성이며, 실기기 차등 프로브가 측정하지만 GPU/FPS 또는 로드 시간
  향상을 주장하지 않습니다.
- SDK가 이제 플러그인 개발자에게 직접 제공됩니다: 모든 프레임워크 릴리스에
  `turboism-sdk-<version>.jar`와 SHA-256 사이드카가 GitHub 전용 개발자 자산으로 포함되고,
  `./gradlew publishToMavenLocal`이 깨끗한 `dev.turboism` 좌표를 생성하며,
  `templates/plugin-template`은 릴리스된 SDK에 대해 빌드되는 독립 프로젝트입니다.
- Warp Deformer Alt Symmetry 플러그인이 바운딩 박스 Alt 의미를 Warp Deformer 컨트롤
  포인트로 확장합니다: Alt 드래그는 수직 그리드 축을 가로질러 미러링하고 Alt+Shift는
  수평 축을 가로질러 미러링합니다. 캔버스 상단 컨트롤 스트립의 기여 토글이 축을
  활성화하고 네이티브 캔버스 힌트가 활성 상태를 표시합니다. 미러링된 위치는 모델 쓰기
  경로로 커밋되어 대칭 이동이 네이티브 Undo/Redo에 합류합니다. 플러그인은
  `turboism.cubism.model.read`, `turboism.cubism.model.write`,
  `turboism.ui.toolbar.contribute`, `turboism.ui.canvas.hint`를 요청하며, 네이티브 미러는
  검증된 호스트 후크가 설치된 경우에만 바인딩됩니다. 검증된 drag-tick 후크는 검토된
  Cubism 5.2.03, 5.3.02, 5.3.03 정확한 아티팩트만 허용하며, 네이티브 드래그 틱 내에서
  라이브 미러링 미리보기와 단일 실행 취소 항목을 제공합니다. 미검토 호스트는 동일한
  결과의 릴리스 시점 AWT 폴백을 유지합니다.
- `Action.of`와 `MenuContribution.of`가 단일 지점 기여 등록을 일반
  `SimpleAction`/`SimpleMenuContribution` 값으로 구축하므로, 플러그인이 기여하는 모든
  액션이나 메뉴 항목에 익명 클래스가 필요하지 않습니다.

### 변경

- 일반 CI가 이제 모든 pull request와 `main`으로의 push에서 `devCheck`와 전체
  `checkCompletedCommit` 스위트를 모두 실행하며, 디스플레이 의존 테스트에는 Xvfb를
  사용합니다. 커버리지 가드는 건너뛰거나 필터링되거나 소프트 실패한 게이트를 거부하고,
  채널 검사는 이제 `main`을 따르며 루트 빌드 입력을 포함합니다.
- Javadoc 존재 검사가 이제 라인 기반 매칭 대신 JDK 17 컴파일러 트리 API를 사용하여,
  암시적 public 인터페이스 메서드와 공개적으로 도달 가능한 중첩 타입을 포함합니다.
  파싱 및 도구 실패는 불완전한 결과로 조용히 폴백하지 않고 검사를 실패시킵니다.
- SDK, 런타임, 공식 플러그인 전반의 공개 API 문서가 이제 이전에 문서화되지 않은 선언을
  설명하고 수명 주기, 소유권, 실패, 결과 의미를 명확히 합니다.
- 내장 코어 플러그인이 프레임워크 셸(`dev.turboism.shell`)로 런타임에 통합되었습니다:
  더 이상 플러그인 JAR로 배포되지 않고, 예약된 `turboism.core` 아이덴티티가 여전히
  구성, 작업, 로그를 귀속하며, 플러그인 관리는 여전히 제거 불가능한 코어 행으로 나열하고,
  플러그인 로드 보고서에는 더 이상 나열되지 않습니다. 공유 상단 메뉴 루트는 라우팅 키로
  "Turboism"을 유지하지만 프레임워크 현지화 이름을 표시합니다.
- 관리형 시작이 이제 기본적으로 Cubism을 ZGC(`-XX:+UseZGC`)로 실행합니다: 실기기 A/B에서
  G1의 초 단위 일시 정지(로드 약 1.4초 → 밀리초 미만, 쓰기 약 0.6초 → 밀리초 미만)를
  제거하고 정상 상태 RSS를 약 3분의 1 줄였습니다. Performance → Cubism JVM 토글은 다음
  시작 시 적용되며 명시적 옵트아웃만 저장합니다.
- CSV 파라미터 일괄 가져오기가 이제 쓰기마다 커밋하는 대신 하나의 작성 트랜잭션 내에서
  실행됩니다. 대형 모델 실기기 A/B에서 Update Parameter Structure 재빌드가 37회에서
  4회(-89%)로, 파라미터 쓰기 중앙값이 4.71ms에서 1.19ms(-75%)로, 35회 쓰기 버스트 동안
  EDT 할당이 약 99.8% 감소했습니다.
- 런타임 읽기는 모든 접근자를 다시 투영하는 대신 SDK 형태의 관찰 심을 통해 버전이 지정된
  읽기 또는 스코프 캡처당 한 번만 호스트를 관찰합니다. 합성 벤치마크에서 읽기 경로 시간
  약 29%, 호출당 할당 약 41~43% 감소를 측정했습니다.
- Cubism 시작 억제(업데이트 확인, 스플래시, 정보 대화 상자 건너뛰기)가 이제 새
  설치와 `config.json`이 아직 없는 Turboism 홈 실행을 포함하여 기본적으로 켜져 있습니다.
  `hooks.startup.skip*` 플래그를 `false`로 설정하거나 안전 모드를 활성화하면
  옵트아웃되며, 스키마가 잘못된 구성은 여전히 페일 클로즈됩니다. Premain 진단은 이제
  런타임 로그 싱크가 설치될 때까지 버퍼링되어 `STARTUP_SUPPRESSION_*` 허용 코드가
  세션 로그에 도달합니다.
- 릴리스 게이트가 이제 현지화를 전체 Cubism 언어 매트릭스에 적용합니다. Marketplace
  플러그인 목록은 선언된 모든 로케일(en, ja, ko, zh-Hans, zh-Hant)에
  `plugin.name`/`plugin.description`을 포함해야 하며, 검토된 zh, ja, ko 노트 번역이
  없으면 릴리스 후보가 거부됩니다. 프레임워크 메시지 카탈로그가 공식 플러그인 완전성
  게이트에 합류하여 이제 `checkIntegration`의 일부로도 실행됩니다.
- 모든 선택적 `PluginContext` 서비스 접근자가 이제 getter에서
  `UnsupportedOperationException`을 던지는 대신 해당 서비스의 `unavailable()` 센티널을
  반환하며, 각 서비스는 탐색용 `isAvailable()`을 노출합니다. 센티널은 여전히 페일
  클로즈됩니다: 도메인이 제공하는 곳(작업 제출, 호스트 읽기, 저장소, 사용자 파일, 메시
  편집, 호스트 대화 상자, 스크립트 실행)에서는 구조화된 실패를 보고하고, 그렇지 않은
  곳에서는 사용 시 안정적인 `UnsupportedOperationException`을 던집니다.
  `ScriptService#available`은 `isAvailable`로 이름이 바뀌어 탐색이 어디서나 하나의
  이름을 갖습니다. **플러그인 API 마이그레이션이 필요할 수 있습니다:** 접근자의
  `UnsupportedOperationException`을 잡던 플러그인은 대신 `isAvailable()`을 호출해야
  합니다.
- 18개의 의미 이벤트 타입이 `dev.turboism.sdk.event.cubism`에서
  `dev.turboism.sdk.cubism.event`로 이동했습니다. 폐기된 패키지는 경계 및 패키지 레이아웃
  검사에서 거부되어 더 이상 사용되지 않는 형태가 회귀할 수 없습니다. **플러그인 API
  마이그레이션이 필요할 수 있습니다:** 이벤트 import를 업데이트하세요.
- 설치 시 호스트 후크는 `META-INF/turboism/hooks`에 선언되고 에이전트가 스캔하며 수동
  배선되지 않습니다: 각 `HookContributor`가 자체 허용 여부를 확인하고 `HookRegistry`를
  통해 install/bind/uninstall을 전달하므로, 새 후크는 에이전트 편집이 아니라 매니페스트
  한 줄과 contributor 클래스 하나입니다. 검증됨/페일 클로즈 허용, 원자적 레코드 추출,
  mesh-mirror premain/bind 수명 주기, `installation=`/`cleanup=` 보고 라인은 변경되지
  않습니다.

### 수정

- 애니메이션 문서, 장면, 트랙, 속성이 이제 객체 그래프 전반에서 플러그인 권한, 스코프
  활성 상태, 문서 세대를 강제합니다. 키프레임 복사는 오래되거나 외부 소스를 거부하면서
  동일 플러그인이 소유한 활성 뷰 간의 유효한 복사는 유지합니다.
- 애니메이션 시간 스케일링이 이제 핸들을 단순히 이동하는 대신 키프레임과 베지어 컨트롤
  핸들 시간에 동일한 아핀 변환을 적용합니다. 소수 핸들 시간과 큰 원점 주변의 항등
  스케일링은 정밀도를 유지하며, 이동 및 복사 동작은 변경되지 않습니다.
- 유지된 `cubismRead()` 및 Clip Mask 수집 서비스가 이제 소유 플러그인 스코프가 닫힌 후
  직접 PSD, clip-mask, texture-atlas, render, workspace, theme 읽기 경로를 포함하여
  액세스를 거부하며, 다른 플러그인을 무효화하지 않습니다.
- 물리 편집기 기여는 플러그인 스코프가 닫힐 때 자동으로 해제됩니다. 닫힌 서비스는 새
  기여를 등록할 수 없으며, 반복된 닫기는 나중 기여를 제거할 수 없습니다.
- 아틀라스 캐시 재사용은 이제 입력 다이제스트를 계산할 수 없을 때(기존 픽셀 예산을
  초과하는 이미지 포함) 원래 재빌드 경로로 폴백합니다. 알 수 없는 다이제스트는 더 이상
  동일하게 비교되어 오래된 픽셀을 반환하지 않으며, 일반적인 변경되지 않은 입력은 재사용
  자격을 유지합니다.
- `Motion3Validator`가 `Version` 및 세그먼트 종류 식별자를 좁히기 전에 정확한 숫자 값을
  확인하여, 해당 필드의 극단적인 지수에서 예외를 던지지 않으면서 소수 및 오버플로
  래핑 값을 거부합니다. 수학적으로 동등한 유효한 표현은 계속 허용됩니다.
- 관리형 Graal 설치가 이제 다른 다운로드를 시작하기 전에 중단된 첫 설치가 남긴 검증된
  현재 버전의 고아 활성화 마커를 조정하여, 전체 다운로드와 프로브 후
  `GRAAL_RUNTIME_RECOVERY_REQUIRED`를 피합니다. 알 수 없는 마커와 기존 롤백 보호는 계속
  보호됩니다.
- 각 `DisposableScope` 등록은 이제 등록 핸들과 스코프 정리가 공유하는 단일 해제 작업을
  소유하며, 반복 또는 동시 닫기를 포함합니다. 동일한 리소스의 별도 등록은 더 이상 서로의
  정리 항목을 제거하지 않습니다.
- 호스트 어댑터 액세스를 받는 3~6개 인수 `CorePluginContext` 편의 생성자가 이제 null
  포인터로 실패하는 대신 기본 경로를 통해 생략된 서비스를 조립합니다. 명시적 서비스와
  엄격한 생성자 오버로드는 기존 계약을 유지합니다.
- 런타임 테스트 픽스처가 이제 임시 호스트 클래스를 동일한 이름의 클래스패스 픽스처와
  분리하고, 헤드리스 대화 상자 검사를 별도의 헤드리스 JVM에서 실행하며, 자체 설정 창
  스코프를 닫아 관련 링크 실패, 모달 정지, 테스트 간 창 누수를 제거했습니다.
- Cubism 5.3.03 에디터 모델 DRAFT 매핑 메타데이터가 이제 참조된 검증 레코드와 일치합니다.
  회귀 검사는 손실 있는 숫자 변환을 허용하지 않고 정확한 개수, 기능 ID, 레코드
  다이제스트를 검증합니다. 이는 새 런타임 매핑을 활성화하지 않습니다.
- 열린 두 문서 간 전환이 이제 새로 바인딩된 모델을 즉시 게시합니다. 이전에는 이전 모델이
  게시된 상태로 남아 평가된 읽기가 새 바인딩 아이덴티티 아래에서 잘못된 모델을 추적하고
  고정할 수 있었습니다.
- 에디터 바인딩 아이덴티티 캐시가 더 이상 세션 수명 동안 닫힌 문서 그래프를 유지하지
  않으며(캐시된 아이덴티티는 이제 약하게 보유됨), 빌린 Core 모델은 바인딩이 사라지면
  해제됩니다.
- 여러 무제한 증가 경로가 이제 제한됩니다: 오버레이 버튼 사이드 테이블은 FIFO로 제거되고,
  recent-preview 메모리 맵과 폴러의 emit 중복 제거 마크는 활성 항목으로 정리되며,
  표시되지 않는 서브트리는 장면 팔레트 폴링과 팔레트 필터 컴포넌트 워커에서 제거됩니다.
- 리플렉션 집약 호스트 경로는 검증된 멤버, 생성자, 소유자 클래스와 영구 미스를 캐시하고,
  mesh-mirror 스캔은 디스패치당 한 번 인덱싱하며, 워크스페이스 `safeSegment` 살균 패턴은
  미리 컴파일되고, 팔레트 툴바는 변경이 없을 때 재레이아웃을 건너뜁니다.
- 로그 편집은 각 패턴이 엄격히 필요로 하는 바이트에 대해 게이트하여 관찰된 항목당 약 7개의
  matcher 할당을 제거합니다. 편집 출력은 변경되지 않습니다.
- 프레임워크 셸이 시스템 클래스로더 폴백이 있는 agent jar 위의 적절한 `URLClassLoader`를
  얻었고, 나머지 모든 창 생성은 `TurboismWindowFactory`를 통해 라우팅됩니다.
- 호스트 검증 및 미리보기 패키징 수정: 파라미터 플러그인 JAR이 오래된 버전 이름 대신
  glob으로 위치하고, 검증 프로브는 선언된 i18n 카탈로그를 제공하며, 워크스페이스 번들은
  정규 agent jar에서 패키징되고, 러너는 로컬 호스트 전송과 Windows 결과 라인을 지원하며,
  닫기 단계 아티팩트가 자동 결과에 포함되고, 플러그인 관리 재시작 후크가 검토된 큐
  인벤토리에 허용되고, Cubism 5.3.03 정확한 호스트가 검증 큐에 합류했습니다.
- 후크 레지스트리가 프로세스 종료 닫기 패스와 늦은 등록이 경합할 때 더 이상 핸들을
  손상하거나 이중으로 닫지 않습니다: 닫기 패스는 스냅샷을 탐색하고 각 항목을 원자적으로
  클레임하며, 패스 중간에 등록된 핸들은 반쯤 닫히지 않고 등록 상태를 유지합니다.
- 에이전트가 부트 클래스 경로에서 후크 매니페스트를 읽으므로, agent JAR 내부에 패키징된
  매니페스트는 프로세스 작업 디렉터리에 관계없이 해석됩니다.

## [0.44.0] - 2026-09-11

### 추가

- Turboism이 이제 배포된 릴리스 API(`api.turboism.dev/v1/releases/stable.json`)에 대해
  안정 업데이트를 확인합니다. 비교는 설치된 패키지에 내장된 권위 있는 빌드 번호를
  사용하므로 더 낮은 빌드가 업데이트로 제공되지 않으며, 동일한 빌드 번호에 다른 버전은
  업데이트가 아닌 아이덴티티 충돌로 처리됩니다. 빌드 번호 이전의 설치는 계속 버전으로만
  비교하며 만들어진 번호가 할당되지 않습니다.
- 사용 가능한 업데이트는 Turboism 도킹 패널의 항목이 아니라 호스트가 자체 오른쪽 하단
  메시지에 사용하는 동일한 표면인 그리기 영역 위의 네이티브 Cubism 힌트로 표시됩니다.
  힌트는 키가 지정되어 더 새로운 제공 빌드가 이전 텍스트를 대체하고, 업데이트가 더 이상
  제공되지 않으면 즉시 사라집니다. 클릭하면 고정된 자사 다운로드 페이지가 열립니다.
  릴리스 피드의 URL이 열리거나 설치되지 않으며, 설치 프로그램이 자동으로 다운로드되거나
  실행되지 않습니다.
- 업데이트 검사기는 비차단이며 24시간에 최대 한 번 실행되고, 수동 검사는 항상 사용
  가능합니다. 자동 검사는 Startup 설정 탭에 자체 영구 토글이 있으며 Cubism 자체 업데이트
  억제와 독립적입니다.
- 플러그인은 `UiHostCapabilityService.notifyCanvasHint`, `notifyDismissibleCanvasHint`,
  `showCanvasHintWhile`과 `CanvasHintNotification`, `CanvasHintHandle`,
  `CanvasHintPosition`, `ConditionalCanvasHint`를 통해 그리기 영역 위에 네이티브 Cubism
  힌트를 표시할 수 있습니다. 표시하려면 새 `turboism.ui.canvas.hint` 권한이 필요합니다.
  이 기능은 검증된 5.2.03, 5.3.02, 5.3.03 호스트 경로를 통해 버전 라우팅되며, 경로를
  해석할 수 없는 호스트에서는 근사하지 않고 사용할 수 없다고 보고합니다.
  [SDK v10 검토](sdk/api-contracts/sdk-api-v10-review.md)를 참조하세요. 이 개정은 순수한
  추가이며 플러그인 마이그레이션이 필요하지 않습니다.
- 게시된 릴리스 노트가 이제 간체 중국어와 일본어 외에 검토된 한국어 텍스트도 포함합니다.
  릴리스 문서는 이를 `notesByLanguage`로 노출하고, 영어는 번역이 누락될 때 웹사이트가
  표시하는 폴백으로 남아 있으며, Nightly 제목과 경고는 네 가지 언어 모두로 번역되고 원시
  커밋 제목은 원래 언어로 명시적으로 레이블됩니다.

### 변경

- 검토된 SDK 정확한 기준선이 이제 v10이며 canvas-hint 커밋에 고정되었습니다. 이 개정은
  42개의 API 레코드를 추가하고 제거하거나 변경하지 않습니다. v9와 v8은 모든 릴리스가
  계속 실행하는 과거 정확한 감사로 남아 있습니다.
- WebDAV 백업 플러그인이 `backup`에서 `webdav-backup`으로 이름이 변경되었습니다: Gradle
  모듈과 Java 패키지는 `webdav-backup`/`dev.turboism.plugin.webdavbackup`, 설치 프로그램
  아티팩트는 `plugins/webdav-backup.jar`(이전 `plugins/backup.jar`), 플러그인 id는
  `dev.turboism.plugin.webdav`(이전 `dev.turboism.plugin.backup`)이며, 메뉴 항목은 이제
  현지화됩니다. `backup/webdav.cfg`에 저장된 엔드포인트 설정은 영향을 받지 않습니다.
- 검토된 매트릭스 외부의 언어를 지정하는 `release-notes/<version>.json` 파일이 이제 조용히
  삭제되는 대신 릴리스를 실패시키므로, 오타로 번역이 누락된 릴리스를 배포할 수 없습니다.

### 수정

- 릴리스 API가 GitHub에 도달할 수 없는 동안 모든 채널에 "unavailable"로 응답하는 대신
  마지막으로 검증된 릴리스 스냅샷을 계속 제공합니다. 스냅샷은 최대 24시간 사용 가능하며,
  한 채널의 일시적 실패가 해당 채널의 이전에 검증된 데이터를 삭제하지 않고, 실패한
  새로 고침은 이전 스냅샷을 보존합니다. 게시는 여전히 API에 즉시 알리고, 새로운 예약된
  모니터가 확인된 중복 제거된 인시던트를 보고합니다.
- Windows 설치 프로그램이 사전 설치된 Java 없이 관리형 Graal 런타임을 프로비저닝합니다:
  아카이브를 직접 다운로드하고 검증하며, 독립 실행형 설치를 위한 전체 런타임 구성을
  초기화하고, Graal 페이지는 더 이상 쓸모없는 Java 전제 조건을 주장하지 않습니다.
  프로비저닝 경로는 Windows PowerShell 5.1과 7에서 검증되었습니다.
- 기존 설치를 업그레이드해도 더 이상 두 개의 WebDAV 플러그인 항목이 남지 않습니다. 두
  설치 프로그램 모두 관리형 업그레이드 중에 내장된 플러그인 id로 `plugins/`에서 오래된
  이름 변경 전 JAR을 제거하므로(모든 파일 이름 포함) 이전 id `dev.turboism.plugin.backup`이
  폐기/대체 경계에 합류했습니다: 런타임이 로드를 거부하고, 플러그인 관리가 나열하지
  않으며, `config.json` `disabledPlugins`가 더 이상 유지하지 않습니다. 이름 변경 전
  WebDAV 설정 대화 상자도 항상 중국어를 표시하는 대신 모든 레이블, 버튼, 도구 설명,
  상태 메시지를 현지화합니다.

## [0.43.11] - 2026-09-11

### 추가

- 설치 프로그램이 이제 호스트 로케일에만 의존하는 대신 명시적 언어 선택을 제공하며,
  한국어가 영어, 간체 중국어, 일본어에 합류했습니다. NSIS 마법사는 환영 페이지 전에
  표준 언어 대화 상자를 표시하고 호스트 언어에 관계없이 모든 로케일을 나열합니다.
  IzPack 설치 프로그램은 `kor` 언어 팩, 라이선스 리소스, 모달 언어 팩 선택기를 제공합니다.
  선택한 설치 프로그램 언어는 설치 프로그램 범위에 머물며 `config.json`에 기록되지
  않습니다.
- `GET /v1/downloads/<version>.json`이 릴리스당 다운로드 요청 시작을 보고합니다. 공식
  미러 시작은 GitHub의 바이너리 `download_count`에 추가되고, 응답은 바이너리당 하나의
  `assets` 행을 이름, 키, SHA-256, official, GitHub, total 값과 함께 전달하여 릴리스
  합계와 조정됩니다. 체크섬 사이드카, HEAD/304, 실패한 요청, 0이 아닌 재개 범위,
  verification 접두사 트래픽은 제외되고, 알 수 없는 소스는 날조된 0을 출력하는 대신
  `null`로 유지됩니다.
- Stable, Beta, Nightly 릴리스가 이제 영어를 폴백으로 하는 검토된 간체 중국어 및 일본어
  노트(`notesByLanguage`)를 포함하고, 웹사이트는 로컬에서 언어를 선택합니다. 다이제스트가
  정확한 영어 섹션과 더 이상 일치하지 않는 번역은 재사용되지 않고 거부됩니다. Nightly는
  후보가 준비되는 동안 게시된 조상 기준선과 실제 커밋 제목을 동결하므로, 이후에 도착하는
  커밋은 이미 빌드된 후보가 말하는 것을 변경할 수 없습니다.
- 검토된 번역은 기록을 다시 쓰지 않고도 과거 릴리스를 풍부하게 합니다: 0.43.10과
  0.43.10-0.nightly.3은 정확한 릴리스 ID, 소스 개정, 원래 표시 본문에 바인딩된 표시
  보충을 받으며, 공개 Release 본문, 태그, 영수증, 파일, 빌드 번호는 그대로 유지됩니다.
- 프레임워크 메시지 카탈로그가 이제 `verifyFrameworkCatalogs`에 의해 공식 플러그인과
  동일한 로케일 매트릭스를 유지합니다. 플러그인은 이미 불완전한 카탈로그 세트에서 크게
  실패하지만, 프레임워크는 `ResourceBundle`을 통해 크롬을 해석하여 누락된 카탈로그가
  조용히 영어로 저하되었습니다. 새 게이트는 검증된 루트 외부의 카탈로그를 제공하는
  프레임워크 모듈도 거부합니다.

### 변경

- Beta 및 Nightly 후보는 동결된 노트 컨텍스트(`schemaVersion: 2`)를 기록하고, 승격은
  Stable 노트를 정확한 `CHANGELOG.md` 섹션과 검토된 번역 다이제스트에 바인딩합니다.
  체크아웃 후 `CHANGELOG.md`, `release-notes/` 또는 노트 모듈이 변경된 후보는 검토되지
  않은 노트를 게시하는 대신 페일 클로즈됩니다.
- Java 제거 프로그램은 `config.json`을 유지하는 것이 기본값으로 NSIS 제거 프로그램과
  일치하며, 속성 없는 헤드리스 또는 콘솔 실행도 유지합니다.
- 검토된 SDK v9 정확한 앵커가 호스트 로케일 수정으로 이동하여 SDK 계약이 시작 프로그램의
  DISPLAY 로케일이 아닌 적용된 언어를 유지합니다. 정규 API 덤프는 변경되지 않으며
  `UiHostCapabilityService.hostLocale()`의 기본 본문 바이트만 이동했고, v2~v8 과거
  앵커는 감사된 상태로 유지됩니다.

### 수정

- 플러그인 UI 언어가 이제 Cubism Editor의 File → Environment Settings → General →
  Language에서 선택한 언어를 따릅니다. 시작 프로그램의 `-Duser.language`는 빌드의 언어
  버전만 선택하고 런타임에 변경되지 않으므로 더 이상 호스트 언어로 처리되지 않습니다.
  Cubism이 이 런타임이 연결된 후 저장된 설정을 프로세스 기본 로케일에 적용하므로, 유효
  로케일은 검증된 호스트가 ACTIVE가 되면 다시 해석됩니다. 명시적 `-Dturboism.locale` 또는
  `config.json` 로케일이 여전히 호스트보다 우선합니다.
- 프레임워크 자체 `ResourceBundle` 카탈로그가 이제 완전한 zh-Hans/zh-Hant/en/ja/ko
  매트릭스를 포함합니다. `dev.turboism.ui.panel`에는 `messages_en.properties`와
  `messages_zh_Hans.properties`가 없어 간체 중국어 호스트가 조용히 레거시 스크립트 없는
  `messages_zh.properties`로 폴백했습니다. 해당 카탈로그는 선택적 호환성 별칭으로
  유지되지만 더 이상 스크립트 접미사가 있는 카탈로그를 대신할 수 없습니다.
- 툴바 아이콘은 플러그인이 제공하는 디스플레이 스케일 변형(125/150/175/200%)을 통해
  로드되고 하나의 다중 해상도 아이콘으로 해석되므로, 설치 프로그램 항목이 더 이상 고해상도
  디스플레이에서 단일 스케일되지 않은 비트맵에서 그려지지 않습니다.
- Java 제거 프로그램의 확인 대화 상자의 한국어 분기가 영어 텍스트로 폴백하는 대신
  현지화되었고, 네 개의 README 템플릿이 이제 제거 프로그램의 기본 유지 `config.json`
  확인란을 기본 삭제 대신 설명합니다.

## [0.43.10] - 2026-09-09

### 추가

- 명시적 스케일 계약과 가드된 네이티브 패킹 통합을 통한 현재 페이지 텍스처 아틀라스
  패킹.
- 안정적인 탐색과 구성 가능한 코어 툴바 아이콘을 갖춘 캡처된 의미 기록 타임라인.
- 독립적인 릴리스 API, GitHub 릴리스 동기화, 검증된 스트리밍 미러, 전역으로 할당된
  제품 빌드 아이덴티티.

### 변경

- 검토된 SDK v9 정확한 릴리스 기준선을 채택하고 v8을 과거 감사로 강등했습니다. 앵커는
  하나의 표시 필드를 추가합니다: `CubismOperationEvent`에 선택적 `label`이 추가되어
  관찰된 네이티브 Cubism Editor 편집이 현지화 가능한 네이티브 편집 이름을 전달할 수
  있습니다. 구성 요소는 `subjectId` 뒤에 추가되며 명시적으로 아이덴티티가 아닙니다.
  **플러그인 API 마이그레이션이 필요할 수 있습니다:** `CubismOperationEvent`를 직접
  구성하는 플러그인은 새로운 다섯 번째 구성 요소를 전달해야 합니다.
  [SDK v9 검토](sdk/api-contracts/sdk-api-v9-review.md)를 참조하세요.
- 검토된 SDK v8 정확한 릴리스 기준선을 채택하고 v7을 과거 감사로 강등했습니다. 앵커는
  v7 게이트가 기록하지 않은 캡처된 의미 타임라인과 UI 표면, 그리고 네이티브 에디터
  편집을 위한 네 개의 새로운 `CubismOperation` 아이덴티티(`SET_HIERARCHY_PARENT`,
  `DETACH_HIERARCHY_PARENT`, `MOVE_DRAWABLE`, `SET_DRAWABLE_COLOR`, 기존 상수 서수가
  이동하지 않도록 추가됨)를 캡처합니다. **플러그인 API 마이그레이션이 필요할 수
  있습니다:** `HistoryEntry`, `RuntimeSettings`, `PanelView.Toggle` 생성자가
  변경되었습니다. [SDK v8 검토](sdk/api-contracts/sdk-api-v8-review.md)를 참조하세요.
- 제품 릴리스를 읽기 전용 후보 빌드와 명시적 보호 GitHub 승격으로 분할했습니다. 실패한
  후보 시도는 의도된 버전을 재사용하고, 승격만 공식 주석 태그를 만들고 다시 빌드하지
  않고 검증된 바이트를 게시합니다.
- 검토된 SDK v8 현재 페이지 텍스처 레이아웃 계약을 채택했습니다.
- 네 가지 언어의 프로젝트 및 설치 문서와 작업 내 로컬 호스트 검증 감독을 추가했습니다.

### 수정

- 다중 버전 Scene 팔레트 브리지를 강화하고, 정확한 Cubism 5.3.03 라우팅을 추가하고,
  통합된 팔레트 정리를 유지했습니다.
- 코어 툴바 이미지를 호스트 홈 아이콘 크기와 일치시켰습니다.
- 호스트 검증 환경 처리와 잘못된 결과 거부를 강화했습니다.

## [0.43.9] - 2026-09-06

이 릴리스는 게시되지 않은 0.43.4~0.43.8 후보를 대체합니다.

### 추가

- 동기 작성 트랜잭션, 그룹화된 Glue 쓰기, 안정적인 기록 항목/트랜잭션 아이덴티티,
  확장된 형식화된 MCP 읽기/쓰기 작업, JAR 파일에서 직접 플러그인 설치, 복원된 바운딩
  박스 오버레이 컨트롤.

### 변경

- v2~v6 과거 기준선을 유지하면서 검토된 SDK v7 정확한 릴리스 기준선을 채택했습니다.
  **플러그인 API 마이그레이션이 필요할 수 있습니다:** `McpHttpConnection`은 이제
  endpoint/protocol만 받고 더 이상 `authorization()`을 노출하지 않습니다. `HistoryEntry`는
  추가 아이덴티티 필드를 가지며 예외 열거형 서수가 변경되었습니다.
  [SDK v7 검토](sdk/api-contracts/sdk-api-v7-review.md)를 참조하세요.
- 팔레트 툴바/필터 기여를 통합하고 반복되는 모델 및 기록 스캔을 줄였습니다.
- 로컬 루프백 MCP 서버가 베어러 인증 없이 작동합니다.
- 릴리스 패키지에서 관리형 fx 런타임 바이트와 개발 전용 Turboism with fx 플러그인을
  제외합니다.

### 수정

- 스냅샷 경합 및 비동기 플러그인 비활성화 회귀 테스트를 안정화하고, 바운딩 박스 초안
  메타데이터를 수정하고, 네이티브 Java 설치 프로그램 Full 페이로드 테스트를 Windows
  정책 범위에서 분리하면서 프로덕션 검증을 약화시키지 않았습니다.
- 페이로드 변경 전 설치 프로그램 구성 검증, 파라미터 일괄 전송 행 레이아웃 수정, SDK
  인터페이스 프록시 범위, Cubism 5.3.03 텍스처 아틀라스 자동 레이아웃 후크 선택, MCP/
  런타임 트랜잭션 및 수명 주기 수정을 포함합니다.

## [0.43.8] - 2026-09-06

이 릴리스는 게시되지 않은 0.43.4~0.43.7 후보를 대체합니다.

### 추가

- 동기 작성 트랜잭션, 그룹화된 Glue 쓰기, 확장된 형식화된 MCP 읽기/쓰기 작업, JAR
  파일에서 직접 플러그인 설치, 복원된 바운딩 박스 오버레이 컨트롤.

### 변경

- 팔레트 툴바/필터 기여를 통합하고 반복되는 모델 및 기록 스캔을 줄였습니다.
- 로컬 루프백 MCP 서버가 베어러 인증 없이 작동합니다.
- 릴리스 패키지에서 관리형 fx 런타임 바이트와 개발 전용 Turboism with fx 플러그인을
  제외합니다.

### 수정

- 릴리스 검증 수정: 결정론적 스냅샷 경합 및 비동기 플러그인 비활성화 테스트, 스키마
  준수 바운딩 박스 초안 메타데이터, 별도의 Windows 정책 범위를 가진 네이티브 OS Java
  설치 프로그램 Full 페이로드 테스트. 이러한 검증 수정으로 프로덕션 검증, 수명 주기
  동작 또는 호스트 선택기는 변경되지 않았습니다.
- 페이로드 변경 전 설치 프로그램 구성 검증, 파라미터 일괄 전송 행 레이아웃 수정, SDK
  인터페이스 프록시 범위, Cubism 5.3.03 텍스처 아틀라스 자동 레이아웃 후크 선택, MCP/
  런타임 트랜잭션 및 수명 주기 수정을 포함합니다.

## [0.43.7] - 2026-09-06

이 릴리스는 게시되지 않은 0.43.4~0.43.6 후보를 대체합니다.

### 추가

- 동기 작성 트랜잭션, 그룹화된 Glue 쓰기, 확장된 형식화된 MCP 읽기/쓰기 작업, JAR
  파일에서 직접 플러그인 설치, 복원된 바운딩 박스 오버레이 컨트롤.

### 변경

- 팔레트 툴바/필터 기여를 통합하고 반복되는 모델 및 기록 스캔을 줄였습니다.
- 로컬 루프백 MCP 서버가 베어러 인증 없이 작동합니다.
- 릴리스 패키지에서 관리형 fx 런타임 바이트와 개발 전용 Turboism with fx 플러그인을
  제외합니다.

### 수정

- Cubism 5.3.03 바운딩 박스 초안 매핑 메타데이터를 기존 스키마에 맞게 수정했습니다.
  선택기와 검증 레코드는 변경되지 않았으며 초안은 확인되지 않은 상태로 유지됩니다.
- 스냅샷 경합 및 비동기 플러그인 비활성화 회귀 테스트를 프로덕션 검증 또는 수명 주기
  동작을 변경하지 않고 결정론적으로 만들었습니다.
- 페이로드 변경 전 설치 프로그램 구성 검증, 파라미터 일괄 전송 행 레이아웃 수정, SDK
  인터페이스 프록시 범위, Cubism 5.3.03 텍스처 아틀라스 자동 레이아웃 후크 선택, MCP/
  런타임 트랜잭션 및 수명 주기 수정을 포함합니다.

## [0.43.6] - 2026-09-06

이 릴리스는 게시되지 않은 0.43.4 및 0.43.5 후보를 대체합니다.

### 추가

- 동기 작성 트랜잭션 범위, 그룹화된 Glue 쓰기, 확장된 형식화된 MCP 읽기/쓰기 작업,
  JAR 파일에서 직접 플러그인 설치, 복원된 바운딩 박스 오버레이 컨트롤.

### 변경

- 프로덕션 호스트 수명 주기 처리로 팔레트 툴바 및 필터 기여를 통합하고 반복되는 모델
  및 기록 스캔을 줄였습니다.
- 로컬 루프백 MCP 서버가 베어러 인증 없이 작동합니다.
- 릴리스 패키지에 더 이상 관리형 fx 런타임 바이트 또는 개발 전용 Turboism with fx
  플러그인이 포함되지 않습니다.

### 수정

- 릴리스 회귀 범위 안정화: 스냅샷 복사 경합은 변경된 타임스탬프와 보존된 타임스탬프를
  명시적으로 포함하고, 반복된 플러그인 비활성화 테스트는 콜백 진입이 아닌 종료 수명
  주기 완료를 기다립니다. 이러한 테스트 수정으로 프로덕션 검증 및 수명 주기 동작은
  변경되지 않습니다.
- 페이로드 변경 전 설치 프로그램 구성 검증, 파라미터 일괄 전송 행 레이아웃 수정, SDK
  인터페이스 프록시 범위, Cubism 5.3.03 텍스처 아틀라스 자동 레이아웃 후크 선택, MCP/
  런타임 트랜잭션 및 수명 주기 수정을 포함합니다.

## [0.43.5] - 2026-09-06

이 릴리스는 게시되지 않은 0.43.4 후보를 대체하며 해당 변경 사항을 아래에 포함합니다.

### 추가

- 동기 작성 트랜잭션 범위, 그룹화된 Glue 쓰기, 확장된 형식화된 MCP 읽기/쓰기 작업,
  JAR 파일에서 직접 플러그인 설치, 복원된 바운딩 박스 오버레이 컨트롤을 추가했습니다.

### 변경

- 프로덕션 호스트 수명 주기 처리로 팔레트 툴바 및 필터 기여를 통합하고 반복되는 모델
  및 기록 스캔을 줄였습니다.
- 로컬 루프백 MCP 서버를 베어러 인증 없이 작동하도록 변경했습니다.
- 모든 릴리스 패키지에서 관리형 fx 런타임 바이트와 개발 전용 Turboism with fx
  플러그인을 제거했습니다.

### 수정

- 스냅샷 복사 ABA 회귀 테스트를 파일 시스템 타임스탬프 해상도와 독립적으로 만들어
  프로덕션 검증을 약화시키지 않으면서 관찰 가능한 파일 변경과 타임스탬프가 변경되지
  않은 손상된 스냅샷을 모두 포함합니다.
- 페이로드 변경 전 설치 프로그램 구성 검증, 파라미터 일괄 전송 행 레이아웃 수정, SDK
  인터페이스 프록시 범위, Cubism 5.3.03 텍스처 아틀라스 자동 레이아웃 후크 선택, MCP/
  런타임 트랜잭션 및 수명 주기 수정을 포함합니다.

## [0.43.4] - 2026-09-06

### 추가

- 그룹화된 Glue 쓰기를 포함한 동기 작성 트랜잭션 범위와 확장된 형식화된 MCP 읽기/쓰기
  작업을 추가했습니다.
- JAR 파일에서 직접 플러그인 설치를 추가하고 바운딩 박스 오버레이 컨트롤을 복원했습니다.

### 변경

- 프로덕션 호스트 수명 주기 처리로 팔레트 툴바 및 필터 기여를 통합했습니다.
- 로컬 루프백 MCP 서버를 베어러 인증 없이 작동하도록 변경했습니다.
- 런타임 핫 경로에서 반복되는 모델 및 기록 스캔을 줄였습니다.

### 수정

- Windows 및 Java 설치 프로그램이 페이로드 변경 전에 `config.json`을 검증하거나
  마이그레이션하고, 정수 스키마 토큰과 런타임 유효 v1 값을 요구하며, 관련 없는 설정을
  덮어쓰지 않고 업그레이드 플러그인 선택을 적용하도록 했습니다.
- 모든 릴리스 채널에서 관리형 fx 런타임 페이로드를 제거하고 개발 전용 Turboism with
  fx 플러그인이 어떤 릴리스 패키지에도 제공되지 않음을 문서화했습니다. 선택적 Windows
  fx 해석기 테스트가 개인 픽스처 경로가 없을 때 안전하게 건너뛰도록 했습니다.
- 겹치는 파라미터 일괄 전송 바인딩 행, SDK 인터페이스 프록시 범위, Cubism 5.3.03
  텍스처 아틀라스 자동 레이아웃 후크 선택을 수정했습니다.
- 트랜잭션 결과 및 수명 주기 처리에 관한 MCP 및 런타임 검토 결과를 해결했습니다.

## [0.43.3] - 2026-09-03

### 추가

- Full 설치 프로그램 및 아카이브에 관리형 Windows x64 fx v0.0.5 제품 페이로드를 추가하고,
  시작 전에 정확한 크기와 SHA-256으로 검증됩니다. 업스트림이 Windows 복구 아카이브를
  제공하지 않으므로 Windows 제품 복구 또는 재설치가 이를 복원합니다.
- Core About 창의 Turboism 제목 아래에 현지화된 "For you, a bouquet" 헌사를 추가했습니다.
- 리포지토리 루트와 공개 설치 프로그램에 간체 중국어를 정본으로 하는 완전한 Turboism
  최종 사용자 실행 선언 v2.0을 추가하고, 프로젝트 아이덴티티, 합법적인 Cubism 권한,
  사용자 콘텐츠 백업, 현상 유지 작동에 대한 네 가지 별도 필수 확인을 포함합니다.
- 구성, 서비스, 호스트 어댑터, 플러그인 로딩, 최종 보고를 위한 간결한 시작 단계 지속
  시간 진단을 추가했습니다.
- Turboism MCP Connection 창을 추가하여 현재 로컬 MCP 주소, 베어러 토큰, 명시적 복사
  작업, 베어러 값이나 MCP 세션 식별자를 기록하지 않는 제한된 프로세스 로컬 연결 및
  요청 기록을 표시합니다.
- 안정적인 기본 MCP 포트 `43123`을 추가하고(`turboism.mcp.port=0`은 여전히 임시 포트를
  선택) 검증된 Claude Code, Visual Studio Code, Codex CLI 구성 예제를 포함합니다.
- 직접적인 **Turboism → fx Settings** 메뉴 항목을 추가하여 MCP 또는 ACP 연결이 없어도
  런타임 경로, fx 소유 셸, 공급자 설정에 도달할 수 있게 했습니다.
- 모달 추가, 편집, 제거, 선택 대화 상자가 있는 저장된 fx 공급자 프로필을 추가했습니다:
  fx 소유 Vercel, Codex, Grok 기본 제공은 정확한 fx 로그인 명령을 시작하고, 사용자 지정
  OpenAI 호환 또는 자체 호스팅 엔드포인트는 Turboism 소유 루프백 어댑터가 제공합니다.
- 사용자 지정 공급자 프로필에 대한 최선의 `/v1/models` 검색과 모달 수동 모델 ID 대화
  상자를 추가했습니다.
- 사용자 지정 공급자 API 키 영구 저장소를 추가하여 키를 한 번만 입력합니다: 성공하면
  현재 사용자에 대해 Windows DPAPI로 보호되고, 그렇지 않으면 플러그인 자체 구성
  디렉터리의 `auth.json`에 기록됩니다.
- 결정론적 Windows 설치 프로그램 페이로드 처리를 추가하여 SHA-256으로 변경되지 않은
  JAR과 fx 제품 페이로드를 건너뛰고 더 이상 빈 완료 페이지를 표시하지 않습니다.
- PSD Clip Mask Import 진행률 보고를 추가하고 가져오기 실행 중 재진입을 방지했습니다.
- 영구적인 설치 프로그램 시작 프로그램 및 관리형 하위 프로세스 진단을 추가하고 MCP
  파라미터 및 모델 쓰기 결과, 명시적 바인딩 존재, 커밋된 생성을 재시도 안전하고
  런타임 진단과 상관시켰습니다.

### 변경

- Core About 창이 권위 있는 Gradle 릴리스 버전에서 생성된 프레임워크 버전을 표시하도록
  했습니다.
- 릴리스 플러그인 허용 목록을 수정하여 History Panel 및 PSD Clip Mask Import 비즈니스
  플러그인을 게시하면서 개발 셸, 데모, 레거시 자리 표시자를 공개 설치 프로그램 및
  아카이브에서 제외했습니다.
- Windows 설치 프로그램을 확대하고 수동 구성기를 크기 조정 및 최대화 가능하게 만들었으며,
  설치는 이제 정확히 지원되는 Cubism Editor 설치(5.2.03, 5.3.02, 5.3.03)를 발견하고,
  발견된 모든 호환 설치를 선택하고, 구성기를 열지 않고 선택한 Turboism 바로 가기 및
  해시 가드된 공식 BAT 컨트롤을 헤드리스로 적용합니다.
- 선택적 관리형 GraalVM 설치 실패 또는 취소를 나머지 Turboism 설치를 중단하지 않고
  표시하고 기록하도록 했습니다.
- 일반 하단 상태 알림을 최신 메시지 슬롯으로 정의하고 호출 플러그인의 범위 지정
  Turboism 로거를 통해 모든 상태 호출을 기록했습니다. 소형 상주 메트릭은 독립적인
  키 슬롯을 유지합니다.
- 모든 공식 UI Theme 로케일에서 테마를 적용한 후 Cubism Editor를 다시 시작해야 올바르게
  렌더링됨을 명확히 했습니다.
- Windows fx 후보가 Turboism의 정확한 인증된 숫자 루프백 HTTP MCP 서버만 허용하고
  공식 Linux/macOS fx 자산과의 내구성 세션, 네이티브 도구, 일반 네트워킹, 프로세스 또는
  지속성 패리티를 주장하지 않음을 문서화했습니다.
- Clip Mask Viewer가 현지화된 로딩 상태를 즉시 표시하고 분리된 관계 인덱싱, 개수,
  분석, 그래프 프로젝션을 Cubism 호스트 스레드에서 벗어나 취소 및 오래된 결과 가드와
  함께 이동하도록 했습니다.
- fx v0.0.5에 Claude 구독 로그인이 없고 Claude Pro/Max 또는 Claude Code 구독이
  Anthropic API 자격 증명이 아니므로 해당 공급자 프로필이 제공되지 않음을 문서화했습니다.
  사용자 지정 어댑터는 OpenAI Chat Completions만 구현합니다.
- fx의 Gateway 추론 수준이 추측된 `reasoning_effort`로 번역되는 대신 의도적으로
  OpenAI 호환 엔드포인트로 전달되지 않음을 문서화했습니다.
- fx Agent 창이 공급자, 모델 또는 호환성 선택 없이 자동으로 연결되도록 했습니다. 누락된
  공급자/모델 설정은 이제 사용자가 프롬프트를 보낼 때만 보고됩니다.
- 사용자 지정 공급자 기본 모델을 선택 사항으로 만들고 **Use**가 선택한 프로필에 즉시
  다시 연결되도록 했습니다.

### 수정

- 모델 객체 생성을 Cubism 런타임과 일치시키고 런타임 진단 리소스를 연결 상태와 분리하여
  MCP 도구 쓰기가 호스트 모델 작업과 경합하는 문제를 수정했습니다.
- MCP 쓰기가 재시도에서 살아남지 못하고, 파라미터 바인딩 및 커밋된 생성이 잘못 보고되고,
  잘못된 런타임 요청이 안전하지 않게 기록되는 문제를 수정했습니다. 쓰기 경고는 이제
  런타임 진단과 상관되고 적용된 스키마는 바인딩 런타임과 일치합니다.
- PSD Clip Mask Import가 네임스페이스 간 모델 id를 비교하여 다른 장면의 동일한 id에서
  오작동할 수 있는 문제를 수정했습니다. id 비교는 이제 하나의 네임스페이스 내에 머물고
  가져오기는 재진입 없이 진행 상황을 보고합니다.
- 페이로드 SHA-256 작업 후 Windows 설치 프로그램 완료 페이지가 비어 표시되는 문제를
  수정하고, 설치 프로그램 시작 프로그램 진단이 동시 레이블된 stdout/stderr를 안정적으로
  배출하도록 유지했습니다.
- Windows 제거 프로그램 구성 유지 확인란이 외부 마법사 창에 연결되어 안정적인 상호
  작용을 방해하고 확인 페이지를 느리게 만들 수 있는 문제를 수정했습니다.
- 제거 옵션을 명확하고 기본 활성화된 "Keep config.json" 동작으로 변경했습니다.
  구성은 사용자가 지울 때만 삭제됩니다.
- 정확한 버전 SDK 허용을 유지하면서 Cubism Editor 5.2.03에서 History 스냅샷 가용성을
  복원했습니다.
- Java가 사용 가능한 ACL 뷰 또는 기존 사용자별 Windows 경로 및 재분석 검사를 노출할 때
  Windows MCP 시작을 복원하고, 베어러 값, 엔드포인트 또는 개인 연결 파일 경로를 기록하지
  않습니다.
- 동시 레이블된 stdout 및 stderr 배출을 포함한 영구적인 구성기 및 관리형 GraalVM 하위
  프로세스 진단을 추가하여 실패한 BAT 통합 및 선택적 런타임 설정을 실행 가능하게
  만들었습니다.
- 플러그인 활성화가 빈 플러그인별 config, data, cache 디렉터리를 생성하지 못하도록
  했습니다. 저장소는 이제 첫 번째 실제 작업에 필요한 디렉터리만 생성하고, 플러그인
  로그는 공유 런타임 로그를 사용하며, 사용되지 않는 `palette-filter-attach.tsv` 진단은
  더 이상 생성되지 않습니다.
- 지원되지 않는 리플렉션 캔버스 외부 다시 그리기 시도를 제거했습니다. 테마 변경은 이제
  Cubism Editor 5.2.03 및 5.3.02에서 확인된 신뢰할 수 있는 다시 시작 필요 동작을
  사용합니다.
- fx Settings 창이 fx 셸을 열기 전에 설정된 연결이 필요하여 새 설치에서 공급자 및 모델
  설정에 도달할 수 없었던 문제를 수정했습니다.
- 사용자 지정 엔드포인트 어댑터가 fx가 모든 요청에서 보내는 reasoning 필드를 거부하고,
  `ai-language-model-id` 요청 헤더를 무시하고, 인증되지 않은 자체 호스팅 엔드포인트에
  API 키를 요구하는 문제를 수정했습니다.
- 사용자 지정 프로필이 플러그인 소유 Gateway 전용 fx 홈과 선택적 직접 `--model` 인수로
  적응된 연결을 시작하여 사용자의 일반 fx 홈에서 관련 없는 Codex 또는 Grok 선택을
  상속하는 문제를 수정했습니다.
- Turboism이 사용 가능한 공급자나 모델이 선택되지 않았다고 보고하기 전에 프롬프트
  텍스트가 지워지는 문제를 수정했습니다.
- ACP MCP 서버를 거부한 제한된 Windows fx 페이로드를 Turboism의 정확한 인증된 숫자
  루프백 HTTP MCP 서버로 제한된 독립적으로 재현 가능한 빌드로 교체했습니다.
- 관리형 Windows 시작이 경로를 포함하는 Turboism JVM 옵션을 독립 실행형 `cmd.exe`
  명령으로 재분석하는 문제를 수정했습니다. 옵션은 이제 임시 Cubism BAT에 인용된 인수로
  삽입되고 상속된 Java 옵션 변수와 오래된 Turboism 통합 블록은 하위 프로세스에서
  제외됩니다.

## [0.43.2] - 2026-08-29

### 수정

- Cubism의 번들 JVM이 선택적 `java.net.http` 모듈을 노출하지 않을 때 Turboism 런타임
  시작이 Core 플러그인 UI 등록 전에 중단되는 문제를 수정했습니다. 관리형 GraalVM
  컨트롤은 이제 메뉴, 도구 모음 항목 또는 패널을 비활성화하지 않고 페일 클로즈됩니다.
- 관리형 GraalVM 전체 다운로드 기한을 20분에서 4시간으로 연장하여 느리지만 지속적으로
  진행되는 Windows 다운로드가 조기에 종료되지 않도록 했습니다.

## [0.43.1] - 2026-08-29

### 변경

- Windows 구성기의 독립 바로 가기 및 바로 가기 인수 모드를 명확히 하고, 두 모드 모두
  공식 Cubism BAT 파일을 수정하지 않으며, 생성된 관리형 `.lnk` 파일 이름에 공백이
  없고, Turboism 디렉터리를 여는 설치 완료 옵션을 추가했습니다.
- `logs/installer/managed-graal-install.log`에 영구적인 관리형 GraalVM 설치 진행 및
  진단을 추가했습니다.

### 수정

- 대소문자를 구분하지 않는 `$home` 변수가 PowerShell의 읽기 전용 `$HOME` 자동 변수와
  충돌할 때 Windows PowerShell 시작 및 관리형 GraalVM 도우미가 실패하는 문제를
  수정했습니다.
- OpenJDK가 Windows에서 null `BasicFileAttributes.fileKey()`를 보고하기 때문에 관리형
  GraalVM 설치가 일반 Windows 파일 및 디렉터리를 거부하는 문제를 수정했습니다. Windows는
  이제 사용할 수 없는 키를 요구하지 않고 파일 형식, 크기, 재분석 지점 상태를 다시
  검증합니다.

## [0.43.0] - 2026-08-28

### 추가

- Cubism Editor 5.3.03에 대한 정확한 버전 런타임, SDK 가용성, 작성, 기록, 수명 주기,
  텍스처 아틀라스 지원.
- ACP 통합, 내구성 세션 복구, 지원되는 Linux 및 macOS Java 설치 프로그램 패키지에 대한
  검토된 관리형 fx 런타임 페이로드를 포함한 Turboism with fx.

### 변경

- 지원되는 Windows 호스트 플랫폼, 정확한 Cubism Editor 버전, 설치 선택 사항, 현재 기능,
  검증 지침으로 GitHub README를 확장했습니다.
- 모든 GitHub Release가 `CHANGELOG.md`의 일치하는 버전 섹션에서 노트를 가져오는 회귀
  검사를 추가했습니다.
- 재사용 가능한 테스트 지원 및 교차 모듈 통합 테스트를 `testing/` 아래로 그룹화하고
  검토된 SDK API 계약을 `sdk/` 도메인 아래로 이동했습니다.
- 공개 Cubism 호환성 계약을 무시된 로컬 Cubism 참조 및 호스트 증거와 분리하고 생성된
  참조 보고서를 `build/reports/` 아래로 이동했습니다.
- 리포지토리 위생을 강화하여 로컬 참조, 연구, AI 검토 증거, 생성된 보고서 및 검증 출력
  경로의 강제 추가를 거부합니다.
- Windows 안전 및 Java 설치 프로그램 페이로드 스테이징을 분할하여 관리형 네이티브 fx
  런타임을 검토된 Linux 및 macOS Java 패키지로 제한했습니다.
- 결정론적 릴리스 계획, 정확한 후보 및 페이로드 검증, 변경 불가능한 8개 자산
  프레임워크 계약, 조정된 Plugin Directory 및 Updates 게시 순서를 추가했습니다.

### 수정

- 오래된 문서 바인딩을 거부하고 호스트 객체 그래프를 누출하지 않고 네이티브 바인딩을
  유지하면서 레거시 SDK 기록 구현을 보존했습니다.
- 파라미터 그룹 및 바인딩 일괄 액세스를 선형으로 확장하고, 모든 네이티브 텍스처 아틀라스
  항목을 보존하고, 아틀라스 해석기 및 뷰를 하나의 호스트 세대에 바인딩했습니다.
- 관리형 런타임 수명 주기, ACP 내구성 세션 재생, 설치 프로그램 플랫폼 정책, 로컬 JSON
  파싱(ASCII 전용 JSON 숫자 및 유니코드 이스케이프 포함)을 강화했습니다.
- 소유자 전용 권한 또는 보호된 Windows DACL 증명을 설정할 수 없는 경우 MCP 베어러 게시를
  페일 클로즈로 만들었습니다.
- 게시 전에 정확한 플러그인 명단, Windows/Java 페이로드 분리, 모든 릴리스 검증기 호출
  사이트를 검증했습니다.

### 알려진 제한 사항

- Windows 패키지에는 관리형 네이티브 fx 런타임이 포함되지 않습니다. 네이티브 보호
  DACL 및 재분석 안전 베어러 파일 게시 메커니즘이 사용 가능해질 때까지 MCP 지원
  Turboism with fx는 Windows에서 사용할 수 없습니다.
- 관리형 하위 프로세스 정리는 네이티브 Unix 프로세스 그룹 및 Windows Job Object
  격리가 구현될 때까지 최선 노력으로 유지됩니다.
- 게시된 바이너리는 코드 서명되거나 공증되지 않습니다. 다운로드 후 함께 제공되는
  SHA-256 사이드카를 확인하세요.

## [0.42.0] - 2026-08-25

### 추가

- Live2D Cubism Editor용 Java 17 에이전트 런타임 및 공개 플러그인 SDK.
- Cubism Editor 5.2.03 및 5.3.02용 정확한 버전 런타임 어댑터.
- Windows NSIS 설치 프로그램, Lite 및 Full ZIP 배포, 크로스 플랫폼 IzPack 설치 프로그램.
- 플러그인 수명 주기, 권한, 구성, 현지화, 작업, 이벤트, 액션, 메뉴, 도구 모음,
  워크스페이스, Cubism 통합 서비스가 포함된 공식 자사 플러그인 번들.
- 결정론적 릴리스 메타데이터가 있는 플러그인 패키지 검사 및 Plugin Directory 통합.
- 모든 게시된 설치 프로그램 및 아카이브의 SHA-256 사이드카.

### 변경

- 공개 SDK 거버넌스를 정확한 Cubism Editor 가용성 주석이 있는 하나의 릴리스된 API
  계층으로 통합했습니다.
- 런타임 이벤트 전달을 통합하고 플러그인 활성화, 교체, 분해 및 실패 격리를 강화했습니다.
- 릴리스 패키징이 하나의 공유 스테이징된 페이로드와 단일 검토된 플러그인 허용 목록을
  사용하도록 했습니다.

### 수정

- 정확한 호스트 후크, 공개 이벤트 검증, 플러그인 종료, 백업 계속 펜싱, 최근 미리보기
  호버 축소판을 안정화했습니다.
- 패키지 검사, 구성 병합, 경로 처리, 보고서 편집, 공급망 검증을 강화했습니다.

### 알려진 제한 사항

- Windows는 기본 Cubism 호스트 플랫폼입니다.
- Java 설치 프로그램은 macOS 및 Linux에서 사용할 수 있지만 macOS Cubism 호스트 준비는
  주장되지 않으며 Linux Cubism 호스팅은 지원되지 않습니다.
- 이 릴리스의 게시된 바이너리는 코드 서명되거나 공증되지 않습니다. 설치 전에 함께
  제공되는 SHA-256 사이드카를 확인하세요.

[Unreleased]: https://github.com/Turboism/Turboism/compare/v0.44.0...HEAD
[0.44.0]: https://github.com/Turboism/Turboism/releases/tag/v0.44.0
[0.43.11]: https://github.com/Turboism/Turboism/releases/tag/v0.43.11
[0.43.10]: https://github.com/Turboism/Turboism/releases/tag/v0.43.10
[0.43.9]: https://github.com/Turboism/Turboism/releases/tag/v0.43.9
[0.43.8]: https://github.com/Turboism/Turboism/releases/tag/v0.43.8
[0.43.7]: https://github.com/Turboism/Turboism/releases/tag/v0.43.7
[0.43.6]: https://github.com/Turboism/Turboism/releases/tag/v0.43.6
[0.43.5]: https://github.com/Turboism/Turboism/releases/tag/v0.43.5
[0.43.4]: https://github.com/Turboism/Turboism/releases/tag/v0.43.4
[0.43.3]: https://github.com/Turboism/Turboism/releases/tag/v0.43.3
[0.43.2]: https://github.com/Turboism/Turboism/releases/tag/v0.43.2
[0.43.1]: https://github.com/Turboism/Turboism/releases/tag/v0.43.1
[0.43.0]: https://github.com/Turboism/Turboism/releases/tag/v0.43.0
[0.42.0]: https://github.com/Turboism/Turboism/releases/tag/v0.42.0
