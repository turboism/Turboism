---
turboismReadmeSchema: 1
pluginId: dev.turboism.plugin.texture-atlas-stats
version: 0.1.0
kind: feature
status: preview
delivery: store-candidate
category: analysis
tags: texture-atlas, metrics, inspection
turboismApi: "[0.1.0,0.2.0)"
requiresCubism: true
interface: swing
---

# テクスチャアトラス統計

> **Turboism 公式プラグイン** · **ステータス: プレビュー**

Cubism のネイティブテクスチャアトラスエディターで、モデル画像の合計数と現在のテクスチャの画像数を表示します。

| 項目 | 値 |
|---|---|
| バージョン | `0.1.0` |
| プラグイン ID | `dev.turboism.plugin.texture-atlas-stats` |
| カテゴリー | `analysis` |
| タグ | texture-atlas, metrics, inspection |
| Turboism API | `[0.1.0,0.2.0)` |
| Cubism が必要 | はい |
| インターフェース | `swing` |
| ライセンス | プロジェクトライセンス |

## 機能概要

- ローカライズされた統計情報の1行を、アクティブなテクスチャアトラスエディター UI に追加します。
- モデル画像の合計数と選択中のテクスチャの画像数を1秒ごとに更新します。
- 読み取り失敗をホスト UI に伝播させる代わりに、利用不可状態を表示します。

## 要件と互換性

- **Turboism API:** `[0.1.0,0.2.0)`。
- **Cubism:** Cubism が必要です。Turboism は現在、正確にレビュー済みの Editor アーティファクト `5.2.03` および `5.3.02` を受け入れています。このプラグインは、宣言されたサービスと機能が利用可能な場合にのみ、各ホスト向け機能を公開します。
- **インターフェースモード:** `swing`。
- **プラグイン依存関係:** 宣言されていません。

## インストールと有効化

この公式プラグインは**ストア候補**であり、まだ公開済みのストア掲載ではありません。マーケットプレイスで公開されるまで、Turboism の公式リリースパッケージからインストールし、**Plugin Management** で有効にしてください。このワークフローが不要な場合は、同じウィンドウから無効化またはアンインストールしてください。

## 使い方

1. モデルを開き、Cubism のテクスチャアトラスエディターを起動します。
2. ネイティブエディターウィンドウに追加された統計情報の行を見つけます。
3. 選択するテクスチャを切り替えると、現在のテクスチャの数が更新されます。合計は1秒ごとに更新されます。

## 機能

| 宣言された機能 | ユーザーへの影響 |
|---|---|
| `cubism.texture-atlas.statistics` | アクティブなテクスチャアトラスエディターセッションからモデル画像数を読み取ります。 |

## 権限

| 権限 | スコープ | 要求理由 |
|---|---|---|
| `turboism.cubism.model.read` | `application` | アクティブなテクスチャアトラスエディターセッションからモデル画像数を読み取ります。 |

## プライバシーとデータ

### ネットワーク

ネットワーク接続は行いません。

### ローカルデータ

データを永続化しません。アクティブなテクスチャアトラスセッションの概要だけを読み取り、メモリ内の UI ラベルを更新します。

### テレメトリー

このプラグインはテレメトリーを送信しません。

プラグイン ID が付加されたプラグインのライフサイクル記録と障害記録が、Turboism のセッションログおよび Cubism のホストログに記録されることがあります。

## 状態と制限

- **ステータス:** プレビュー。
- レビュー済みのテクスチャアトラスセッションおよびエディター UI サービスが必要です。
- このプラグインは読み取り専用です。エディターまたはサービスが利用できない場合、この行は表示されないか、ローカライズされた利用不可状態を表示します。

## トラブルシューティング

| 症状 | 確認事項 |
|---|---|
| 統計情報の行がない | ネイティブテクスチャアトラスエディターを開き、レビュー済みホストでプラグインが有効であることを確認してください。 |
| カウントがゼロのまま | モデルとテクスチャがアクティブであることを確認し、次の1秒ごとの更新を待ってください。 |
| 利用不可のテキストが表示される | テクスチャアトラスセッションの読み取り失敗について Turboism ログを確認してください。 |

## サポートとライセンス

- **プロジェクトウェブサイト:** [https://turboism.dev](https://turboism.dev)
- **発行者:** Turboism Contributors
- **ライセンス:** プロジェクトライセンス
- **プラグイン ID:** `dev.turboism.plugin.texture-atlas-stats`
