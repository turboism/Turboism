---
turboismReadmeSchema: 1
pluginId: dev.turboism.plugin.palette-label-style
version: 0.1.0
kind: feature
status: preview
delivery: store-candidate
category: appearance
tags: palette, labels, typography
turboismApi: "[0.1.0,0.2.0)"
requiresCubism: true
interface: embedded
---

# パレットラベルスタイルプラグイン

> **Turboism 公式プラグイン** · **ステータス: プレビュー**

デフォーマ、パーツ、パラメーターパレットのコンテキストメニューに、テキストおよび背景色のコントロールを追加します。

| 詳細 | 値 |
|---|---|
| バージョン | `0.1.0` |
| プラグイン ID | `dev.turboism.plugin.palette-label-style` |
| カテゴリー | `appearance` |
| タグ | palette, labels, typography |
| Turboism API | `[0.1.0,0.2.0)` |
| Cubism が必要 | はい |
| インターフェース | `embedded` |
| ライセンス | プロジェクトライセンス |

## 機能概要

- 対応するパレット項目に、プリセット、クリア、カスタムカラーの操作を提供します。
- 一時的なパレット外観オーバーライドと、ネイティブなデフォーマラベル色の書き込みを調整します。
- 対応するモデルまたはプロジェクトがアクティブになったとき、プロジェクトごとの色を再適用します。

## 要件と互換性

- **Turboism API:** `[0.1.0,0.2.0)`。
- **Cubism:** Cubism が必要です。Turboism は現在、レビュー済みの正確な Editor アーティファクト `5.2.03` と `5.3.02` を受け入れます。本プラグインは、宣言されたサービスと機能が利用できる場合にのみ、各ホスト向け機能を公開します。
- **インターフェースモード:** `embedded`。
- **プラグイン依存関係:** 宣言されていません。

## インストールと有効化

この公式プラグインは、まだ公開ストアに掲載されていない**ストア候補**です。マーケットプレイスで公開されるまでの間は、Turboism の公式リリースパッケージを通じてインストールし、**Plugin Management** で有効化してください。このワークフローが不要な場合は、同じウィンドウから無効化またはアンインストールできます。

## 使い方

1. 対応するデフォーマ、パーツ、またはパラメーターパレットで項目を選択し、そのコンテキストメニューを開きます。
2. テキストカラーまたは背景カラーのサブメニューを選択し、プリセット、クリア操作、またはカスタムカラーを選びます。
3. プロジェクトを再度開き、同じプロジェクトおよびオブジェクト ID に対して保存済みの色が再適用されることを確認します。

## 機能

| 宣言された機能 | ユーザーへの効果 |
|---|---|
| `ui.context-menu.contribute` | 対応するパレットコンテキストメニューに色のサブメニューを追加します。 |
| `ui.appearance.modify` | 一時的なパレットのテキスト/背景オーバーライドを適用します。 |
| `ui.dialog.contribute` | カスタムカラーフォームを開きます。 |
| `cubism.model.read` | 選択されたオブジェクトと再適用対象を解決します。 |
| `cubism.model.write` | ネイティブなデフォーマラベル色を書き込みます。 |
| `cubism.project.read` | 永続化の範囲をアクティブなプロジェクトに限定します。 |
| `file.read` | プロジェクトごとに保存された色を読み取ります。 |
| `file.write` | プロジェクトごとに保存された色を書き込み、またはクリアします。 |

## 権限

| 権限 | スコープ | 要求する理由 |
|---|---|---|
| `turboism.action.register` | `application` | パレットコンテキストメニューから呼び出すラベルテキスト/背景色操作を登録します。 |
| `turboism.ui.context-menu.contribute` | `application` | デフォーマ、パーツ、パラメーターパレットのコンテキストメニューに、テキストカラーと背景カラーのサブメニューを追加します。 |
| `turboism.ui.appearance.modify` | `application` | パレット項目のテキストと背景色をオーバーライドし、ネイティブなデフォーマラベル色を設定します。 |
| `turboism.ui.dialog.contribute` | `application` | カスタムカラーフォームダイアログを開きます。 |
| `turboism.cubism.model.write` | `application` | デフォーマタブ背景メニュー用にネイティブなデフォーマラベル色を書き込みます。 |
| `turboism.cubism.model.read` | `application` | 選択されたパレットオブジェクトを解決し、モデルを開いた際に保存済みの色を再適用します。 |
| `turboism.cubism.project.read` | `application` | プロジェクトごとのラベル色永続化のため、アクティブなプロジェクト ID を解決します。 |
| `turboism.file.read` | `application` | 再適用のために、プロジェクトごとに保存されたラベル色を読み取ります。 |
| `turboism.file.write` | `application` | 各適用またはクリアの後、プロジェクトごとのラベルテキスト/背景色を永続化します。 |

## プライバシーとデータ

### ネットワーク

ネットワーク接続を行いません。

### ローカルデータ

プロジェクトごとの色レコードを、`palette-label-style/colors-<projectId>.properties` としてプラグインデータにアトミックに保存します。レコードには、プロジェクト/オブジェクト識別子、パレットファミリー、色の対象、`#RRGGBB` 値が含まれます。

### テレメトリー

このプラグインからテレメトリーが送信されることはありません。

プラグイン ID を付して、プラグインのライフサイクルおよび障害の記録が Turboism のセッションログと Cubism のホストログに表示される場合があります。

## 状態と制限

- **ステータス:** プレビュー。
- アクティブなプロジェクト/モデルと、対応するパレットコンテキストメニューおよび外観サービスが必要です。
- 無効な保存済みレコードは無視されます。ネイティブなデフォーマ色と一時的なパレットオーバーライドは別々のホストパスを使用するため、可用性が異なる場合があります。

## トラブルシューティング

| 症状 | 確認事項 |
|---|---|
| 色のサブメニューがない | 対応するパレット項目を使用し、コンテキストメニューおよび外観機能が利用可能であることを確認します。 |
| 色が復元されない | 同じプロジェクトとオブジェクト ID がアクティブであることを確認し、ストレージ/読み取り診断を確認します。 |
| カスタムカラーが拒否される | 提供されるダイアログで、有効な 6 桁の RGB カラーを入力します。 |

## サポートとライセンス

- **プロジェクト Web サイト:** [https://turboism.dev](https://turboism.dev)
- **発行者:** Turboism Contributors
- **ライセンス:** プロジェクトライセンス
- **プラグイン ID:** `dev.turboism.plugin.palette-label-style`
