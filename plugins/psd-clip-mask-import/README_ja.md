---
turboismReadmeSchema: 1
pluginId: dev.turboism.plugin.psd-clip-mask-import
version: 0.1.0
kind: feature
status: preview
delivery: store-candidate
category: workflow
tags: psd, import, clip-mask
turboismApi: "[0.1.0,0.2.0)"
requiresCubism: true
interface: embedded
---

# PSD クリッピングマスクインポートプラグイン

> **Turboism 公式プラグイン** · **ステータス: プレビュー**

明示的なプレビューと上書き確認の後、順序付き PSD クリッピング関係を ArtMesh のクリッピングマスク割り当てへインポートします。

| 詳細 | 値 |
|---|---|
| バージョン | `0.1.0` |
| プラグイン ID | `dev.turboism.plugin.psd-clip-mask-import` |
| カテゴリ | `workflow` |
| タグ | psd, import, clip-mask |
| Turboism API | `[0.1.0,0.2.0)` |
| Cubism が必要 | はい |
| インターフェース | `embedded` |
| ライセンス | プロジェクトライセンス |

## 機能概要

- Turboism パネルに PSD クリッピングマスクインポート領域とインポートボタンを追加します。
- アクティブな Cubism モデルの PSD ドキュメントから順序付き関係を読み取り、関連する ArtMesh を解決します。
- 書き込み前に対象、マスク順序、PSD 参照、上書き競合、スキップ理由を表示します。
- 確認済み変更を一つの条件付きバッチとして適用し、ホストの全件適用と一回の Undo/Redo を利用します。

## 要件と互換性

- **Turboism API:** `[0.1.0,0.2.0)`。
- **Cubism:** 対応する厳密なバージョンが、アクティブモデルの PSD 関係、クリッピングマスク読み取り、順序付き置換、トランザクション/Undo、埋め込みパネル、ダイアログ、状態通知サービスを提供する必要があります。
- **インターフェースモード:** `embedded`。
- **プラグインの依存関係:** 宣言されていません。

## インストールと有効化

このプラグインは Turboism Full リリースに含まれます。Full パッケージをインストールし、**プラグイン管理**で有効にしてください。現在の厳密なバージョンのホストが、レビュー済みの必要サービスをすべて提供する場合にのみ操作と書き込みを利用できます。

## 使い方

1. 対象モデルを開き、アクティブな PSD ドキュメントのクリッピング関係がモデル内の ArtMesh に解決できることを確認します。
2. Turboism パネルの PSD クリッピングマスクインポート領域からインポートを開始します。
3. 対象、マスク順序、ソースレイヤー、スキップ項目、上書き競合を確認します。
4. キャンセル時は書き込みません。明示的に確認した場合のみ表示された割り当てを置き換えます。
5. Cubism の Undo/Redo で確認済みバッチを取り消し、または復元します。

## 機能

| 宣言された機能 | ユーザーへの効果 |
|---|---|
| `cubism.psd.layer-relationship.read` | アクティブなドキュメントの順序付き PSD クリッピング関係を読み取ります。 |
| `cubism.clipmask.read` | 現在の ArtMesh クリッピングマスクリストと反転状態を読み取ります。 |
| `ui.dialog.contribute` | プレビューと上書き確認ダイアログを表示します。 |
| `ui.status.notify` | インポート、スキップ、書き込みなし、安全な失敗を報告します。 |
| `cubism.clipmask.replace-ordered-sources` | 確認済みの ArtMesh マスクソースを計画順に置き換えます。 |
| `cubism.transaction.real-write-undo` | Cubism の実書き込み/Undo 経路でバッチを適用します。 |
| `ui.embedded-panel.contribute` | Turboism パネルにインポート領域を追加します。 |

## 権限

| 権限 | スコープ | 要求する理由 |
|---|---|---|
| `turboism.cubism.model.read` | `application` | 順序付き PSD レイヤー関係を読み取り、既存 Drawable ID に解決します。 |
| `turboism.cubism.model.write` | `application` | 確認済みクリッピングマスク割り当てを条件付きモデルバッチで原子的にコミットします。 |
| `turboism.action.register` | `application` | PSD クリッピングマスクインポートアクションを登録します。 |
| `turboism.ui.panel.contribute` | `application` | PSD クリッピングマスクインポートボタンを提供します。 |
| `turboism.ui.dialog.contribute` | `application` | 書き込み前に対象、マスク、競合、スキップを表示します。 |
| `turboism.ui.status.notify` | `application` | インポート、スキップ、失敗、フェイルクローズ診断件数を報告します。 |

## プライバシーとデータ

### ネットワーク

ネットワーク接続は行いません。

### ローカルデータ

ファイルシステムまたはプラグインストレージへのアクセスを要求せず、データを永続化しません。アクティブな PSD 関係、ArtMesh ID、現在の順序付きマスクリストと反転状態のみを読み取り、明示的に確認された割り当てのみを一つの条件付きバッチで書き込みます。

### テレメトリー

このプラグインからテレメトリーは送信されません。

プラグイン ID 付きのライフサイクルおよび安全な失敗の記録が Turboism セッションログと Cubism ホストログに表示される場合があります。ユーザー向けエラーはホスト例外の詳細ではなく、ローカライズされた安全なメッセージを使用します。

## 状態と制限

- **ステータス:** プレビューリリースプラグイン。
- クリッピング関係のみをインポートします。PSD 画像やテクスチャのインポート、ArtMesh 作成、レイヤーバインド修復、キャンバス拡張、一般的な PSD 再インポートは行いません。
- 置換ごとに確認が必要です。既存の非空リストまたは反転状態は上書き競合として表示され、結果は非反転になります。
- 未解決、マスク不足、自己参照、重複 ID、ドキュメント間の曖昧さ、既に一致する関係はスキップまたはフェイルクローズになります。
- コミット直前にモデルを再取得し、ドキュメント、モデル ID、計画がプレビューと一致することを検証します。変更がある場合は書き込みません。
- ホストは宣言された Cubism/UI サービスを提供する必要があり、Cubism 外では利用できません。

## トラブルシューティング

| 症状 | 確認事項 |
|---|---|
| インポート領域またはボタンがない | プラグインが有効で、現在の対応 Cubism バージョンで埋め込みパネルとアクションサービスが利用可能か確認してください。 |
| プレビューがスキップ項目だけになる | PSD のベース、マスク、対象がアクティブモデル内の一意な ArtMesh に解決できるか確認してください。 |
| 確認しても変更されない | 既に一致する項目、キャンセル、曖昧さ、またはドキュメント/モデル/計画変更による安全な失敗を確認してください。 |
| インポートが安全に失敗する | ローカライズされた状態概要とプラグインログを確認し、PSD、クリッピングマスク、ダイアログ、書き込み/Undo、状態サービスが利用可能か確認してください。 |

## サポートとライセンス

- **プロジェクト Web サイト:** [https://turboism.dev](https://turboism.dev)
- **公開者:** Turboism Contributors
- **ライセンス:** プロジェクトライセンス
- **プラグイン ID:** `dev.turboism.plugin.psd-clip-mask-import`
