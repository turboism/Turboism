package dev.turboism.plugin.textureatlas;

/** Persisted plugin-owned automatic-layout algorithm policy (bridged from the native dialog). */
public enum TextureAtlasLayoutAlgorithm {
    NATIVE,
    MAXRECTS
}
