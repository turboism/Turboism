# Native resource workload (diagnostic, not an optimization)

Exact Cubism5.3.02, bundled JVM17, existing reviewed JAR/BAT and heavy fixture hashes in [experiment ledger](NATIVE-PERFORMANCE-EXPERIMENTS.md). Product flags all off. The JDK-only auxiliary is excluded from product packaging. Reuses the existing common runner and proc CPU/DRM/RSS/PSS sampler, not a second launcher.

## Workload

After task-document readiness, collect300 seconds with the existing memory protocol:30 seconds natural idle;60 native modeling zoom-in/out commands paced at least500ms with actual camera-scale changes; exact original raw camera-scale restoration through native setter/listeners, camera update and repaint; dirty/Undo history unchanged;30 seconds restored idle; native close of the single clean task document;120 seconds natural closed-document idle. Remaining sampler time is labeled by explicit phase boundaries, not counted as zoom. The observer retains only a weak document reference after closing, no image graph or host model snapshots. No force-GC, model saving, changed heap limits or skipped rendering. A weak reference not cleared without a natural collection is inconclusive, not proof of a leak. P03-b in the ledger retains the failed120-action/UI-scale-restore protocol; never combine its timings as an identical run.

Current P03-c amendment: before sampler ready, normalize the task-copy camera to a native scale step using one zoom-in/out pair. Record original fit scale separately. “Original” in the timed workload means this normalized starting scale, not the arbitrary initial fit ratio. The latter was not exactly restorable through native reciprocal/listener updates in P03-b/c; both failures remain recorded. Timed actions still require exact raw and derived scale restoration, not a relaxed tolerance.

The current public SDK command enum does not expose modeling zoom-in/out or document-close. This auxiliary invokes those exact native commands on EDT; it does not expose reflection to plugins or change product APIs. Each action requires the same document/view and task window. Zoom is not parameter dragging and not window resize. Numerical camera/dirty/Undo checks do not establish every visual pixel or every interactive workflow.

## Build, dry run, execute

```sh
./gradlew --no-daemon --max-workers=1 devCheck checkResourceValidationBundle checkCubismHostValidationArguments
TURBOISM_ENV_FILE=/opt/dev/projects/turboism/.env \
  bash scripts/preview/run-resource-host-validation.sh plain rs-n1 \
  --transport local --fixture-remote "$HOME/Downloads/heavy.cmo3" \
  --fixture-sha256 029e9a4ea13f03afdf956b63f6ee1dfd663bd9046c602b786d359bd1d0c7f80c \
  --dry-run
```

Real execution must use the [shared queue](README-host-validation-scheduling.md), never a direct wrapper/BAT/Java or retired remote Runner. Use a task-local manifest for `native-resource:5302`, prepare from the integrated capability worktree, then submit the verified ID only with user authorization and completed gates. Follow `status/wait/events`; external sessions end naturally. Preparation is not execution or PASS. Use a fresh short label per run. `profile` instead of `plain` adds bounded JFR profile recording (128MiB); these are diagnostic cohorts, not optimization A/B comparisons. Report profiler overhead.

The readonly observer closure is `start-task-memory-observer.sh` plus exactly `measure-task-memory.py`, `host_memory_identity.py` and `host_resource_counters.py` staged under `validation/`. Prepare pins the absolute preparing Python binary as a host dependency; hook exec uses `-I -S -B` and the existing managed-background lifecycle, not self-fork/PID cleanup. System stdlib/OS remain platform dependencies. The observer pins its inherited manager scope directory identity and boot ID, enumerates only its cgroup members, and rejects ambiguous/reused/moved process identity. It cannot signal or clean processes. Unknown hooks remain blocked. Each sample includes scope attribution; counters may be explicitly unavailable, never fabricated zero. N06/I17 records this path's single exact-5302 baseline PASS; it does not admit other hooks/versions or establish optimization benefit.

Primary phase/result file: `state/resource-workload/result.properties`. CPU/GPU/RSS evidence and loading heap/GC remain under the existing `state/texture-upload/memory` protocol and `memory-samples.jsonl`. Optional `state/resource-workload/profile.jfr` is local private evidence; do not commit model contents, JFR or official binaries. All actions, failures, validation results, hypotheses and retry conditions go into P03–P07 in the ledger immediately. PASS requires full common-runner hashes, result, sampler completion, normal exit0 and no forced cleanup; it is never by itself a primary-metric benefit.

The final auxiliary also records `documentWeakClearedAtSamplerEnd`/`weakFinal.epochMillis` after300 seconds, to avoid mistaking an early check for a post-GC result. The native Editor may itself call System.gc; the auxiliary neither adds nor suppresses those calls. See P04-c for the observed native Full GC and remaining reference, without a root-owner claim.

Offline admission smoke (build the auxiliary first): `python3 scripts/test/test_native_resource_policy.py`; requires Linux JDK17 (`JAVA17_HOME` override). Three tests cover six rejected configurations and require FAIL before native workload phases. Missing prerequisites fail instead of silently skipping.

Recorded runs: rs-n1/n2 driver failures; rs-n3, rs-p1, rs-p2 complete runner PASS. They are phase baselines/diagnosis, not optimization A/B. P07's separate offline `WarpInteriorPrototype.java` is a retained negative pure-computation experiment and is not compiled into this auxiliary or the product. All outcomes and exact run IDs are in the ledger.

New queue baseline `nm-scope-b1` completed with 428 samples and natural bound-cgroup destruction. Its late RSS fall coincided with rising process swap and low system memory availability; it is not evidence of freed retained objects. Report RSS/PSS/private/swap and JVM used/committed separately, and match memory pressure in future comparisons. Sampling-call read latency was substantial (median about119ms), not a measured CPU-cost estimate. See N06/I17 for phase statistics, hashes and limits.

## Native released-image attribution (test-only)

The auxiliary now takes two bounded readonly snapshots: after `idle.end` before `zoom.begin`, and after `restored.end` before native close. Exclude `retain.beforeZoom.begin/end` and `retain.beforeClose.begin/end` from idle/zoom timing; this is an amended diagnostic cohort, not an identical historical baseline. Each snapshot verifies raw field shapes and class origins under the already hash-verified host. It reads current model groups and `_filteredImage` directly: never the lazy `getFilteredImage()` or pixel capture/PNG helpers. Coverage is **model-image filtered resources**, not every cache/atlas-only image/GPU allocation.

`retain.beforeZoom.*`/`retain.beforeClose.*` report identity-deduplicated resource/user counts, active/released records, released records whose user is absent from that resource's active set, model-user source association, coverage/status/reason and durationNs. Caps are 4096 groups, 4096 model images/resources and 16384 combined records, with a 250ms cooperative deadline including layout setup. This is not a hard scheduling bound. A resource's relationship counts/cohort are committed only after bounded identity consistency checks; concurrent changes can still evade checks (including ABA), so COMPLETE means completed traversal, **not an atomic heap snapshot**. PARTIAL/UNSUPPORTED are not zero-total conclusions. Counts are not retained bytes or proof that a user has no other legitimate references.

Only scalar Properties and bounded WeakReference cohorts escape capture. Only the before-close cohort persists; `retain.closed.*` and `retain.final.*` count cleared/not-cleared references using `refersTo(null)`, never reacquiring referents. The auxiliary preserves document/view/camera/dirty/Undo guards, adds no native ownership mutations or GC. Overall workload PASS is separate from `retain.attributionStatus=COMPLETE|INCOMPLETE`; incomplete attribution must not be reported as completed optimization acceptance.

## Post-close native ownership attribution (test-only, not yet run on the host)

The auxiliary additionally takes four bounded readonly ownership captures: a control capture while the document is legitimately open and strongly held (`ownership.idle.*`, after `idle.end`), a pre-close capture before the close command (`ownership.beforeClose.*`), and two post-close captures (`ownership.closed120.*` about 120s after the close and `ownership.closedFinal.*` at the sampler end). Exclude `ownership.*.begin/end` from idle/zoom timing; this is an amended diagnostic cohort, not an identical historical baseline.

Each capture counts the static registry list, looks for an entry whose file path string equals the recorded fixture path, and reads that entry's referrer count and sanitised referrer class names, plus the controller current doc/view nullability and the private `viewContextHistory` size, null-element count, non-null-document count and identity-matches to the closed document. Nothing is removed, disposed, cleared or collected; the history field is read with `trySetAccessible` and never written.

The recorded fixture is matched by file path string only, so the probe never has to hold the closed document or its model source. Two accessor families are reported separately because they return different objects: the entry base class `b()`/`c()` describe the entry's loader wrapper, while only the concrete model entry declares `f()` returning the recorded `CModelSource`. Only `fixtureCarriedSourceIsRecordedSource` (identity-only, via `refersTo`) states that the recorded source is still carried by the surviving entry; `fixtureLoadedFlag`/`fixtureWrapperPresent` alone do not.

Every capture reports `status` (`COMPLETE`/`PARTIAL`/`UNSUPPORTED`), `reason`, `durationNs`, `referrersExamined` and the before/after weak-pair samples. Caps are 4096 registry entries, 4096 history views, 16384 combined referrer records and a 250ms cooperative deadline including layout setup; this is not a hard scheduling bound. Counts are staged and published only for a `COMPLETE` capture, so an incomplete capture reports no count at all instead of a partial or fabricated zero, and `PARTIAL`/`UNSUPPORTED` are never zero-total conclusions. `ownership.attributionStatus` is the aggregate: `COMPLETE` only when all four captures are `COMPLETE`, otherwise `INCOMPLETE`; incomplete attribution must not be reported as optimization acceptance. `documentWeakBefore != documentWeakAfter` for one capture invalidates that capture as an observer effect. This remains a bounded, non-atomic traversal: COMPLETE means the traversal finished, not an atomic heap view, and it is not a GC-root walk or retained-size measurement.

Focused offline tests: `python3 -B scripts/test/test_native_image_retain_observation.py`. Synthetic host-shaped objects throw on equals/hashCode/toString/materialization. Tests cover bounded counts, deduplication, unsupported layouts/users, mutation windows, deadline boundaries and weak-state shape without official class loading or forced GC. First exact-5302 ownership run (`nr-own-b1`, 2026-09-10) reached all three pre-`closedFinal` captures: while the document was open the control captures read exactly one registry entry whose `f()` was identity-equal to the recorded model source, and 120s after the close the registry list was empty, the controller history was empty and the current doc/view were null while the 024 resource weak references were still uncleared. The workload itself reported FAIL because the pre-existing memory sampler rejected a 6.93s sample gap during host swap pressure, so the final capture and the aggregate status are missing and this run is **not** an SC-002 PASS. A second run (`nr-own-b2`) reproduced every ownership field exactly and failed the same sampler rule with a 15.2s gap during the zoom window (`SwapFree` a few MB), so the blocker is the pre-existing observer gap rule under host swap exhaustion, not this probe.

Focused offline tests for the ownership captures: `python3 -B scripts/test/test_native_close_ownership_observation.py` (synthetic registry/history graphs, bounds, deadline sweep, mutation windows, accessor-shape rejection and forbidden call sites) plus `python3 -B scripts/test/test_native_resource_policy.py`. Real-host results and all rejected/inconclusive directions are recorded in the experiment ledger.

## Post-close static-root audit (test-only, offline complete, not yet run on the host)

Slices 024/028 narrowed the retained closed graph to "an uncleared document and model source with 848 retained image resources while every observed registry and controller structure is empty". The next observable candidate is the Java static side, so `NativeStaticRootAudit` runs at the same four capture points (`idle`, `beforeClose`, `closed120`, `closedFinal`) and reports under `staticRoots.<phase>.*`. It runs **after** the ownership capture of the same phase: reading a static field may initialize its declaring class, and a class initializer cannot obtain the live model source of an already-open document, so ordering keeps any such initialization out of the ownership observation and makes an observed identity match an application write.

The audited set is frozen in source and reviewed at the exact JAR hash `988ef6a8…f8c84f21`: the four static `CModelSource` caches found by class-file parsing (`appCtrlImpl.ui.a.a#d`, `view.palette.parameter.dialog.G#d`, `view.palette.parameter.dialog.af#i`, `view.palette.parts.a.a#d`) plus 30 reviewed singleton/state statics. Class-file analysis of the 16 947-class JAR found **no** static field of `CModelingDocument`, `doc.IDocument`, `CTextureManager`, `CModelImage`, `CCachedImage` or `ICImageResourceUser`; each of the four source caches is written exactly once and never nulled. `CModelSource` holds a non-final `document` back-pointer and reaches the model images through `textureManager`, so any surviving root of the source also keeps the document and the images alive.

Each entry resolves its owner class without initializing, verifies the loader and code-source origin, reads the declared static field once, and for a non-null holder scans one hop (class and up to four superclasses) for declared fields whose type name is exactly the source or the document type name. An inaccessible or primitive-typed field is reported as `unreadable`, never as a zero-valued holder. Identity is compared only through the 028 `WeakPair` (`refersTo`): no referent is reacquired and no host `equals`/`hashCode`/`toString` is called. No static field is written, nothing is removed/disposed/cleared and no collection or heap snapshot is requested.

Reported scalars are `examined`, `read`, `unreadable`, `nonNull`, `scannedFields`, `holdersWithRecordedSource`, `holdersWithRecordedDocument`, bounded `owner#field`/`owner#field->member` label lists, `durationNs`, `status` and `reason`. Caps are 64 holders, 128 scanned fields and a 250ms cooperative deadline checked before every read. Counts and labels are published only for a `COMPLETE` audit, so `PARTIAL`/`UNSUPPORTED` (`holder-limit`, `field-limit`, `time-limit`, `access`, `layout-or-origin`) report no count at all rather than a fabricated zero. A negative audit excludes only the reviewed Java statics: live-thread and JNI-native roots stay open, and the 44-field review boundary is not a completeness proof.

Focused offline tests: `python3 -B scripts/test/test_native_static_root_audit.py` (synthetic direct/one-hop identity hits, null/foreign/absent/primitive/unreadable negatives, inherited fields, all three bounds and the deadline, forbidden call sites, EDT requirement, foreign-loader refusal and static non-mutation). Exact-host results and the retry conditions are recorded in the experiment ledger (I29).
